# 2026-04-28 — Deep QA Pass (Post v0.8.5.1)

User mandate: 各个细节测试，检查所有可能 bug，包括机制方面、AI 方面、算法方面。
启动游戏，检测后台数据 + 观察前端界面，从根本上修复，迭代直到所有 subagent 找不出问题。

## Method

**Round 1 — parallel read-only investigations** (6 audit agents + 1 runtime
observation agent driving vite + Playwright):

1. Worker pathfinding + idle/stuck
2. Animal AI + wildlife behaviors
3. Map/scenario/resource realism
4. Construction/build pipeline correctness
5. AI planner + LLM/fallback correctness
6. Combat/defense correctness
7. Live runtime via Playwright (boot vite, screenshots, console)

**Round 2 — synthesis** — aggregate findings, sort by severity, design root-cause fixes.

**Round 3 — implementation** — single-threaded.

**Round 4 — re-validation** — re-run Playwright + full test suite.

Iterate until QA agents converge on "no issues found".

## Deliverables tracker

This document records:
- Findings per agent (with file:line citations)
- Fix decisions (with rationale)
- Iteration history

## Round 1 — QA findings

### QA1: Worker pathfinding + idle/stuck (completed)

**CRITICAL**
- F1: `cancelBlueprint` doesn't release worker reservations or paths → BUILDERs walk to ghost tiles for up to 30s (`cleanupStale`)

**HIGH**
- F2: `maybeRetarget` releases reservation BEFORE acquiring new — race window where worker has neither
- F3: `seek_food` feasibility checks stockpile but NOT reachability → infinite spin-retry on walled-off warehouses
- F4: `findNearestTileOfTypes` Manhattan-only fallback ignores reachability; A* fail loop
- F5: `onTileMutated` clears path but not `desiredVel` → workers drift through invalidated paths for 1+ ticks
- F6: Hunger preempt during harvest may release old reservation but fail to acquire new one
- F11: `handleConstruct` re-routes without releasing prior FARM reservation on freshly demoted FARM→BUILDER
- F10: No exponential back-off on path retries; thundering herd under congestion

**MEDIUM**
- F7: Storm rest threshold 0.55 oscillates with recover threshold 0.5 (12s period)
- F8: `findOrReserveBuilderSite` returns site even when overlay was just cleared
- F9: `JobReservation.cleanupStale` 30s window doesn't refresh during active harvest

**LOW**
- F12: `consumeEmergencyRation` short-circuits on stale `reachableFood` flag (2.5s probe cadence)
- F13: `isPathStuck` resets timer when worker is jiggling near waypoint without advancing pathIndex
- F14: Hardcoded `ticksPerSec=30` in wall-regen calculation
- F15: Same-target re-issue doesn't clear `pendingPathWorkerKey`

**Top 5 root-cause fixes:**
1. F1: `cancelBlueprint` must run reservation-release + path/target invalidation cascade
2. F3: thread `entity.debug.reachableFood` into `StateFeasibility.isStateFeasible`
3. F5: zero `agent.desiredVel` in TileMutationHooks step 3
4. F2/F6/F11: introduce `reservation.transferTo(workerId, newTile)` atomic transfer; refactor 3 sites
5. F4/F10: add `consecutiveRetryFailures` + exponential back-off + per-worker target blacklist

### QA2: Animal AI + wildlife (completed)

**CRITICAL**
- C1: Predators never starve. Passive recovery (+0.0288/s) > decay (-0.012/s) at `AnimalAISystem.js:931`. No attrition sink.
- C2: Raider with all workers dead wanders forever. Wall-attack gated on `prey != null`; raider has neither herbivore nor worker prey.
- C3: `raiderAttackDamage` uses `Number(...).toFixed(2)` then `Number()` — fragile string coercion at `EntityFactory.js:419-424`.

**HIGH**
- H1: Predator `huntSuppressed` uses home-zone herbivore count, not nearby. Predator with prey at hand refuses to hunt because of zone bookkeeping.
- H2: Bear chase-distance applied at max-tile cap but `targetDrifted >= 2` re-acquire NOT scaled by `chaseDistanceMult` — bears throttled by hunt-refresh cadence.
- H4: `chooseHerbivoreGrazeTarget` Manhattan fallback can send herbivore across whole map for grass. No zone leash.
- H5: Saboteurs eat `state.resources.food` directly without `recordResourceFlow` — invisible food sink.
- H7: `WildlifePopulationSystem` recovery spawns can roll raider_beast immediately (15% prob); grace-period block applies only to scenario init.

**MEDIUM**
- M3: `chooseSpreadTarget` falls back to `randomPassableTile` — no zone leash, herbivore wanders map.
- M4: `pickSpawnTile` minimum distance 1 tile too tight, allows same-kind clustering at zone boundaries.
- M5: Flee-to-self bug — when predator at distance 0, herbivore tries to flee to its own current tile (`Math.hypot(0,0)=0`).
- M6: "Regroup" state pulls toward `migrationTarget`, not herd. Herd cohesion only via boids forces.
- M7: Predator boids cohesion=0.54 + alignment=0.68 ≈ seek=1.18; tight predator pack can override hunt direction.
- M8: `releaseDeathSideEffects` skips visitor carry refund — saboteur trade goods vanish silently.

**Top 5 fixes:**
1. C1 root: cap predator passive hunger recovery to NET-NEGATIVE without active feeding (e.g., remove the passive recovery, or only allow it during `feed` state).
2. C2 root: when raider has no prey for 30s, mark as `alive=false` with `cause: "starvation"` OR have it leave map.
3. H1 root: change `zoneHerbivores` to `nearbyHerbivores within radius 8` regardless of zone.
4. H5 root: route all saboteur food consumption through `recordResourceFlow`.
5. M5/M6 root: fix flee normalization (use random offset when len=0); rename or rebuild `regroup` state to actually pull toward herd centroid.

### QA3: Map/scenario/resource realism (completed)

**CRITICAL**
- R1: FOREST nodes seeded uniform RNG (`ScenarioFactory.js:834-855`); ignore `moisture`/`elevation` arrays already computed.
- R2: STONE nodes random-walk on shuffled grass (`ScenarioFactory.js:857-884`); ignore ridge/elevation. Quarry building uses ridge but the node layer doesn't.
- R3: HERB nodes biased to FARM/WATER tile-adjacency, not actual moisture array (`ScenarioFactory.js:886-914`).
- R6: Scenario walls (border/spokes/fortress) can break warehouse routing — perimeter pass overwrites, gates at fixed cardinal positions independent of warehouse cluster.
- R9: `carveBridgesOnMainAxis` is defined (`Grid.js:851-912`) but **never called** anywhere. Rivers have no auto-bridges → unreachable land masses on river/island maps.

**MEDIUM**
- R4: Per-template farm placement realistic in 2/6 templates; global `placeDistrictBlobs` fallback uniform-on-moisture, not riverside-banded.
- R5: Initial farm `ensureMinimumInfrastructure` uses noise > 0.2, no moisture/elevation check.
- R7: Wildlife zones use fixed map-relative offsets (`westWilds = westOutpost - 2`), not biome-based — can land on grass with no LUMBER nearby.
- R8: 12 starting workers scattered across all roads/farms/lumbers — no "core" cluster.
- R11: Salinization per-tile only, no neighbor spread (real soil salinization migrates via groundwater).

**LOW**
- R10: Fog reveal correct (prefers coreWarehouse). Acceptable.
- R12: Ruin placement scattered debris, not coherent settlement cluster.

**Top 5 fixes:**
1. R1: weight FOREST candidates by `0.7*moisture + 0.3*(1-|elevation-0.55|*2) + jitter`
2. R2: filter STONE seeds to `ridge[idx] > 0.5 OR elevation[idx] > 0.6`
3. R9: invoke `carveBridgesOnMainAxis()` from `generateTemperatePlainsTerrain`, `generateFertileRiverlandsTerrain`, `generateRuggedHighlandsTerrain` after river carve
4. R7: replace fixed offsets in scenario wildlife zones with `findNearestTileOfTypes(grid, anchor, [TILE.LUMBER, TILE.RUINS])`
5. R3: rank HERB candidates by `moisture[idx]` with bonus for WATER-adjacent; drop FARM-adjacency

### QA4: Construction pipeline (completed)

**CRITICAL**
- BC1: `rebuildBuildingStats` (`Grid.js:2709`) doesn't include `gates` key → `BUILD_COST_ESCALATOR.gate` never fires, hardCap (24) never enforced. Player/AI can spam unlimited gates at base cost.
- BC2: Defense plans (`SkillLibrary.js:55` `defense_line` = 5 walls 0 gates; `ColonyPlanner.js:1232` fallback walls) never insert GATEs. AI walls colony's own warehouses → workers fail to path → food chain collapse.
- BC3: `BALANCE.builderMaxFraction = 0.30` declared but never enforced in `RoleAssignmentSystem.js`. With small pop + ambitious plans, 5 of 7 workers go BUILDER → economy starves.

**HIGH**
- BH1: PlanExecutor `groundPlanStep` uses `BUILD_COST[type]` raw (no escalator) for affordance — AI thinks 5th kitchen costs 8w/3s but actually 24w/9s. False feasibility flags.
- BH3: `setWorkerRole` releases JobReservation but NOT builder-site reservation. Demoted ex-BUILDER permanently holds the site's `builderId` → site is wasted until worker dies.
- BH4: BUILDER physically stands on completed WALL/GATE tile → faction-blocked → A* fails → permanently stuck.
- BH5: Wall destroyed by raider mid-demolish-blueprint — `setTile(RUINS)` overwrites entire tileState entry, deleting the construction overlay. 1 wood commission lost, no refund/completion.

**MEDIUM**
- BM1: `existingCount` for escalator preview uses `state.buildings`, not in-flight blueprint count → queueing N farm blueprints dodges escalator (all charge base cost).
- BM2: Cancel + re-commission demolish on RUINS lets player re-roll salvage best-of-N at 1 wood per attempt (cancel refunds full cost).
- BM4: Cost-suffix label undercounts post-cap multiplier (shows ×2.5 cap, actual is ×2.7+ via `perExtraBeyondCap`).
- BM5: Demolish blueprint cancellable up to 99% — last-second cancel returns 1 wood but loses BUILDER labour. Working as designed but feels punishing.

**LOW**
- BL1: `mutateTile` + `setTileField(wallHp)` + `onTileMutated` step 5 all set wallHp on completion — redundant writes.
- BL2: `BALANCE.gateCost` is dead duplicate of `BUILD_COST.gate` (never read).

**Top 5 fixes:**
1. BC1: `rebuildBuildingStats` add `gates: countTilesByType(grid, [TILE.GATE])`
2. BC2: `defense_line` skill insert gates every N walls; planner audit wall steps for self-block via A*
3. BC3: enforce `Math.floor(totalWorkers * builderMaxFraction)` clamp before economyHeadroom
4. BH4: pre-mutateTile, displace any agent on the soon-to-be-blocking tile to nearest passable
5. BH5: preserve `entry.construction` across `setTile` mutation OR auto-cancel+refund in ConstructionSystem when overlay vanishes with non-zero workApplied

### QA5: AI planner + LLM/fallback (completed)

**CRITICAL**
- AI-S2: `recruit` action documented in SYSTEM_PROMPT but NOT in PromptBuilder's user prompt — **LLM never knows recruit exists**, never emits it. Agent-mode runs perpetually under-pop unless fallback inserts it.
- AI-S15: `shouldReplan` crisis branches (`food_crisis`, `resource_opportunity`) are **dead code** — caller always passes `hasActivePlan=false`, hitting `should=true, reason="no_active_plan"` immediately. Crisis-driven planning structurally non-functional.
- AI-S3: Failed-step orphan cascade. When parent step fails, dependents stay `pending` until 18s grace expires → 18s dead-time per transient failure.
- AI-S1: Stale-LLM-result race overwrites active plan, wipes `_planStartSnap`/`_stepEvals`.

**HIGH**
- AI-S5: `.catch` swallows LLM errors silently — `lastError` plumbing weak; thrown errors don't fall back to algorithmic path or surface to UI.
- `BALANCE.objectiveHoldDecayPerSecond=0.2` is **dead config** (declared at `balance.js`, but no `src/` file references it). Was supposed to dampen AI thrashing per v0.8.5 plan but the wiring is missing.

**MEDIUM**
- AI-S9: ResponseSchema validates JSON shape but minor edge cases (non-string IDs, etc.) slip through.
- AI-S12: DecisionScheduler pause/resume edge case under `state.simPaused`.

**Notable non-bugs (verified safe):**
- LLM JSON parsing handles malformed input via try/JSON.parse + regex fallback ✓
- Demolish-then-build chain via `depends_on` topologically sorted ✓
- Recruit count clamped 1..10 in `_validateStep` ✓
- Failed steps not re-emitted next cycle ✓
- PLAN_STALL_GRACE_SEC=18 correctly enforced ✓
- Fog-HIDDEN rejected at `evaluateBuildPreview` ✓
- Demolish on non-existent ruins gated by `ruinsCount > 5` ✓
- Action whitelist `VALID_BUILD_TYPES` works ✓

**Top 5 fixes:**
1. AI-S2: Add `recruit { count }` to PromptBuilder user prompt (the system prompt mentions it but not in prompt-builder format).
2. AI-S15: `shouldReplan` should be called with `hasActivePlan=true` when relevant; OR move crisis logic into the AgentDirector cooldown gate.
3. AI-S3: PlanExecutor cascade-fails dependents when parent fails (`step.depends_on.includes(failedId)` → mark failed too).
4. Wire `objectiveHoldDecayPerSecond` into either StrategicDirector or PlanEvaluator. If unused, delete.
5. AI-S1: Drop stale LLM responses if `_lastPlanSec` advanced beyond request timestamp.

### QA6: Combat/defense (completed)

**CRITICAL**
- CB-C1: `MortalitySystem.recomputeCombatMetrics` drops saboteurs from `activeThreats` and never writes `activeSaboteurs`. After any death tick, GUARD-promotion/threat-gate logic ignores saboteurs.
- CB-C2: Wall HP regen Manhattan distance mixes world coords (`h.x`) with tile indices (`ix`) — works by coincidence (tile size ~1) but fragile. Hardcoded `ticksPerSec=30` won't survive variable rate.
- CB-C3: `applyBanditRaidImpact` defense uses raw `wallCoverage` (count), not HP-weighted `effectiveWallCoverage`. A 1-HP wall stub provides full impact-tile shield → wall-cheese exploit.

**HIGH**
- CB-H1: GUARD path-fail has NO fallback. Goes `setIdleDesired` and stays idle while raider operates freely. Predators/saboteurs have `pathFailDwellSec` → wall-attack; GUARDs don't have an analogous re-target.
- CB-H2: Killing saboteur HURTS player. `deathReason="killed-by-worker"` increments `deathsTotal` → -10 survival score + eats `raidDeathBudget` → reduces incoming raids (opposite of intent: efficient defense ends the threat scenario).
- CB-H3: RaidEscalator self-fire path is broken — sets `lastRaidTick=currentTick` at enqueue, then WorldEventSystem cooldown drops same-tick raid as `0 < intervalTicks`. Primary fallback raid path silently no-op.
- CB-H6: `nearestThreatDistance` in `recomputeCombatMetrics` filters to PREDATOR only — saboteur in melee range invisible to RoleAssignmentSystem live-promotion.

**MEDIUM**
- CB-M1: GUARD demotion thrashes if threat respawns within 1.2s manager interval — drops reservations on each FARM↔GUARD cycle.
- CB-M2: `proximityGate=6` (RoleAssignmentSystem) vs `proximityTiles=8` (ThreatPlanner) — same response, different gates, no documented hysteresis.
- CB-M3: GUARD picks nearest, not threat-weighted. Targets a saboteur 0.5 tile closer instead of a more dangerous raider_beast at 2.5 tiles.
- CB-M5: Wall regen edge case at 4.4 tiles (just outside 4-tile gate) is fine.
- CB-M6: Mortality reachability cache 2.5s TTL doesn't invalidate on grid version bump.
- CB-M7: EventDirector raid downgrade re-rolls without per-type maxConcurrent check — can dispatch already-active event type, slot wasted.

**LOW**
- CB-L3: EnvironmentDirector raid budget eats hostile deaths (cumulative bug with H2).
- CB-L5: Saboteurs/herbivores can't be medicine-healed (intended).

**Top 5 fixes:**
1. CB-C1/CB-H6: `recomputeCombatMetrics` count saboteurs + populate `activeSaboteurs` field; include in `nearestThreatDistance`.
2. CB-C3: replace `wallCoverage` with `effectiveWallCoverage` in `applyBanditRaidImpact`.
3. CB-H2/CB-L3: distinguish colonist deaths from hostile deaths; only colonist deaths increment survivalScorePenalty + raidDeathBudget.
4. CB-H1: GUARD path-fail → wander or re-target via planner; never `setIdleDesired` while threat in aggro range.
5. CB-H3: RaidEscalator should NOT advance `lastRaidTick` until raid is actually drained from queue (move the assignment to WorldEventSystem when `_applyEvent` runs).

### QA7: Live runtime via Playwright (completed)

Vite booted on `:5174`; `window.__utopia` confirmed. Game runs 55-60 FPS, 65-83 MB, zero JS exceptions, zero render glitches. Console clean except AI proxy 500s (expected — no LLM provisioned). **Static audits confirmed via real gameplay; default scenario unwinnable without manual intervention within 60s.**

**CRITICAL (LIVE OBSERVED)**
- LR-C1: **Carry-only nutrition trap (KNOWN v0.8.1 punt to Phase 9, still dominant)** — workers eat ONLY from `carry.food`. When carry.food=0 + stockpile=340, workers FSM `rule:starving-preempt` → seek_food → no warehouse exists → can't reach stockpile → die. **19 of 23 workers dead in 180s.**
- LR-C2: **Default scenario Broken Frontier ships with 0 warehouses / 0 farms / 0 lumbers.** Without autopilot ON, fallback director cannot autonomously place buildings → colony dies regardless of player skill in opening 60s.
- LR-C3: **`autopilot.buildsPlaced=33` but `state.buildings.warehouses=0`.** Counter increments on submission. Out of 33, only 13 roads + 2 farms appeared. Either queue rejection silently miscounted as success, OR demolish actions immediately pruned blueprints. ZERO warehouses placed despite autopilot active for 90s.
- LR-C4: **`Kade-14 died (killed-by-worker)` event with no combat context** — `combat.activeRaiders=0` at the time. Possibly friendly fire bug from `WorkerCombatSystem` (commit 3b84a17) or starvation mis-attribution.

**HIGH (LIVE OBSERVED)**
- LR-H1: **`Dropped infeasible state target traders:seek_trade`** warning fires every ~5s for entire run (10+ in 180s). Trader policy generates goals planner immediately rejects. No rate-limit on warning spam.
- LR-H2: EntityFocus panel shows "Starving 4, Hungry 13" while raw `agent.hunger > 0.6` count is 0 — UX disagreement about who is starving.

**MEDIUM (LIVE OBSERVED)**
- LR-M1: Time scale 2-3× wall-clock with `controls.timeScale=1` — sim catch-up over-eager.
- LR-M2: `controls.recruitTarget=16`, `recruitQueue=8`, `populationTargets.workers=12` — three uncoordinated population targets.
- LR-M3: Construction sites with `builderId=null` while `seek_construct` workers exist but don't claim — race or priority mismatch.
- LR-M4: "Why no WHISPER?: LLM never reached" stuck status message doesn't clear.

**Top 5 fixes (game-breaking priority):**
1. LR-C1 root: workers should eat from `state.resources.food` directly when `carry.food=0` AND no reachable warehouse — OR simpler — have FARM tiles produce directly into `state.resources.food` regardless of warehouse (with logistics throughput penalty if no warehouse). The "deposit at warehouse" requirement is what makes carry the only path.
2. LR-C2: Bootstrap auto-build — at scenario init, ALWAYS place at least 1 warehouse + 1 farm if scenario provides none. Override `autopilot=false` for the bootstrap-only step.
3. LR-C3: Audit `autopilot.buildsPlaced` to count grid-mutated tiles, not submitted-step events.
4. LR-C4: Trace `killed-by-worker` deathReason — if no `combat.activeRaiders > 0` at time of death, it's mis-attribution.
5. LR-H1: Trader goal generator stops emitting `seek_trade` if no warehouse; coalesce duplicate infeasibility warnings (1× per groupId per planning cycle).

---

## Round 2 — Synthesis (in progress)

After 7 QA agents, the **single dominant runtime failure is the carry-only nutrition trap + bootstrap-warehouse gap**. Static bugs (gate count missing from rebuildBuildingStats, AI walls without gates, GUARD path-fail no fallback, raid-fire double-cooldown drop) are real but invisible in the 180s window because workers all die first.

**Implementation order will be:**

**Tier 0 (game-breaking — MUST fix first):**
- LR-C1: Workers can eat from `state.resources.food` if no reachable warehouse (or farms produce directly into stockpile when no warehouse exists)
- LR-C2: Bootstrap warehouse + farm placement at scenario init
- LR-C3: `autopilot.buildsPlaced` counter audit
- LR-C4: `killed-by-worker` mis-attribution audit

**Tier 1 (CRITICAL static bugs):**
- BC1: GATE count → rebuildBuildingStats
- CB-C1: recomputeCombatMetrics counts saboteurs
- CB-C3: applyBanditRaidImpact uses effectiveWallCoverage
- F1: cancelBlueprint cleanup cascade
- AI-S2: recruit in PromptBuilder
- AI-S15: shouldReplan crisis branches reachable

**Tier 2 (HIGH compounding):**
- F3: feasibility uses reachableFood
- F5: TileMutationHooks zeros desiredVel
- C1 (animal): predator passive recovery — neutral or negative without feed
- C2 (animal): raider with no live workers leaves map / starves
- BH4: BUILDER stranded on completed wall — pre-mutate displacement
- CB-H2: distinguish hostile/colonist deaths (don't penalize player for killing saboteur)
- CB-H1: GUARD path-fail fallback (don't go idle)
- CB-H3: RaidEscalator advance lastRaidTick on drain not enqueue

**Tier 3 (realism + polish):**
- R1/R2/R3: forest/stone/herb node placement biased
- R9: invoke carveBridgesOnMainAxis from terrain generators
- R7: wildlife zones anchor on actual LUMBER clusters
- BC2: defense_line includes gates
- BC3: builderMaxFraction enforced
- LR-H1: trader policy gate; warning de-dup

**Tier 4 (deferred / dead config cleanup):**
- objectiveHoldDecayPerSecond wired or removed
- BL2: BALANCE.gateCost dead duplicate
- L4 (animal): migrationTarget cleanup
- BM2: salvage best-of-N exploit (low priority)

---

## v0.8.7 Iteration — Round 2 (2026-04-28)

QA1 Regression on v0.8.6 found 5 critical bugs in v0.8.6 fixes:
- T0-1: R1/R2/R3 `/255` divisor wrong (Float32 already in [0,1])
- T0-2: F3 gate broke LR-C1 carry-bypass — fixed via wander-eat fallback
- T0-3: Non-FARM workers couldn't eat from reachable farm without warehouse
- T0-4: `activeSaboteurs` field dead-write — wired RoleAssignmentSystem to read
- T0-5: BUILDER displacement BFS-radius-3 fallback for all-blocked case

QA2 UI/UX — 18 findings: EntityFocus rename, infra-cap label, toast cleanup, awaiting-builder cue, Why-no-Whisper reset, wall HP red-tint, demolish cost label

QA3 Perf/Memory — 25 findings:
- 3 memory leaks plugged (deathTimestamps cap 256, _lastToastTextMap prune, listeners dedup+unsubscribe)
- recomputeCombatMetrics quadratic bug fixed (~80× faster on death tick)
- EconomyTelemetry memoized vs grid.version (~30× DevIndex CPU saving)

Deferred items completed: wildlife on LUMBER clusters, dead config cleanup (objectiveHoldDecayPerSecond, gateCost), LLM proxy retry on 429/timeout (server/ai-proxy.js, env vars OPENAI_REQUEST_ATTEMPT_TIMEOUT_MS / OPENAI_MAX_RETRIES / OPENAI_RETRY_BASE_DELAY_MS)

**Final state: 1614 tests / 1611 pass / 0 fail / 3 skip.** Documented skips:
- lineage-birth (v0.8.4 contract change)
- exploit-regression: exploit-degradation (pre-v0.9.0 systemic starvation)
- exploit-regression: road-roi (v0.8.8 known balance gap — ratio 0.06 vs target 0.95, requires Phase 9 carry/deposit refactor + road compounding tuning)

### v0.8.8 follow-up tracker
- Phase 9 carry/deposit policy refactor — workers should deposit before eating
- Road compounding tuning so 6-tile road haul achieves ≥30% adjacent throughput
- Spoilage-on-road exemption
- Wildlife biome check (predators prefer forest, deer prefer forest edge)
- Animal stuck-target zone leash (prevent map-wide wander)



