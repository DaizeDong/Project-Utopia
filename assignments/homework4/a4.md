# Project Utopia HW04

Daize Dong, NetID dd1376

## Current Alpha Diagnosis

HW02 defined the real project goal clearly: this game is about map editing, visible A* reroutes and logistics changes, and interpretable AI decisions layered on top of that simulation. The previous HW04 draft did not stay focused on that goal. It described wrapper UI progress, but it did not honestly describe the actual alpha failures.

The current alpha had three verified blockers before this rewrite:

1. The hosted build link listed in the old HW04 report, `http://152.53.133.32:152/`, was not reachable on March 10, 2026. A dead link is not an alpha deliverable.
2. In the Active phase, the game-state overlay was still intercepting pointer events. That meant the player could not reliably drag the map, click tiles, or inspect entities, which directly broke the main interaction loop.
3. Workers could be pinned into AI or fallback-driven action targets such as `Harvest`, so they kept accumulating carry without reliably switching back to `Deliver`. Resources and objective progress could stall even when the simulation looked busy.

After re-checking the build and codebase, I found a fourth structural problem that is just as important:

4. The default scenario and objective chain did not force the player to edit the map in a meaningful way. Too much infrastructure already existed, so the simulation could drift forward without the player engaging the core mechanic from HW02.

This submission fixes the first-pass alpha blockers and rewrites the production plan around the actual game:

- Active-state overlay hit testing is now disabled during play, so the canvas receives input again.
- Camera interaction is clarified and restored: left click builds/selects, `Alt + Left Click` inspects, and right drag pans.
- Worker delivery is protected from fallback and policy steering, and sticky group targets such as worker `harvest` and trader `trade` are sanitized before runtime.
- Survival states such as `seek_food` are no longer allowed to override non-hungry workers through group policy.
- The map layer now uses three authored scenario families, not one mostly pre-solved default start: `frontier_repair`, `gate_chokepoints`, and `island_relay`.
- Opening objectives are now scenario-specific logistics repairs: reconnect the west lumber route, reopen the basin gates, or bridge the island relay before passive scaling is possible.
- Weather fronts, bandit raids, trade caravans, and animal migration now bind to scenario focus zones instead of acting only as abstract numeric pressure.
- The HUD, Inspector, and Entity Focus panels now expose objective hints, scenario status, tile context, and entity decision summaries so stalls and reroutes are visible during play.

## System-by-System Upgrade Plan

### 1. Input / Camera

**Current weakness**

- Before the fix, the overlay still owned pointer hit-testing after `Start Run`.
- The interaction contract was unclear because right-click inspection conflicted with map drag.

**Why this misses the HW02 goal**

- If the player cannot directly drag, inspect, and edit the map, then the main gameplay loop does not exist.
- Any AI or routing system becomes irrelevant if the player cannot change the world.

**Upgrade plan**

- Keep the Active overlay fully non-interactive unless the run is in Menu or End state.
- Preserve the current interaction mapping: left click build/select, `Alt + Left Click` inspect, right drag pan.
- Add one regression gate for every submission: after `Start Run`, the viewport center must hit the `canvas`, and right drag must move the camera target.

### 2. Economy & Delivery Loop

**Current weakness**

- The original alpha allowed fallback or policy targets to keep workers in local action loops.
- Delivery and other survival-critical local states were not protected strongly enough.
- Group policy could still manufacture false hunger pressure by pushing `seek_food` onto workers who were not actually hungry.

**Why this misses the HW02 goal**

- If logistics do not close, then map edits do not have visible systemic consequences.
- A simulation that cannot reliably move resources from production to storage cannot support meaningful objective design.

**Upgrade plan**

- Keep local state priority strict: `eat`, `deliver`, and feasibility constraints beat group steering.
- Continue sanitizing group targets so they cannot issue sticky local action states such as `harvest`, `trade`, `eat`, `seek_food`, or `idle` for workers and traders.
- Add regression coverage for three cases:
  - workers with carry must eventually enter `deliver`
  - workers with no hunger pressure must not be pulled into `seek_food`
  - resources must increase or decrease in a way that changes objective progress

### 3. Map Editing & Navigation

**Current weakness**

- Earlier builds let the player place structures, but the consequences were inconsistent or too soft.
- The scenario often started with enough roads and buildings that reroute pressure was weak.

**Why this misses the HW02 goal**

- HW02 was not about decorating a map. It was about changing navigation and then observing reroute and redistribution behavior.

**Upgrade plan**

- Keep the sparse authored layouts so each colony begins as a partial network, not a solved network.
- Preserve the family-specific repair steps:
  - reconnect the west lumber route and reclaim the east depot
  - reopen blocked basin gates and secure the granary corridor
  - bridge island causeways and claim the relay depot
- Tune objectives so the player must add roads, storage, and production in order to unlock progression.
- Push more of the difficulty into path structure: warehouses, road continuity, chokepoints, and walls should produce visible A* changes.

### 4. Objectives & Scenario Design

**Current weakness**

- The previous objective chain leaned too hard on abstract colony progression and not enough on spatial intervention.
- The default map could already satisfy too much of the intended buildout.

**Why this misses the HW02 goal**

- If the player can succeed by waiting, then the project stops being a map-editing systems game.

**Upgrade plan**

- Keep the new first objective as a scenario-specific logistics repair objective.
- Keep the second objective as a resource refill objective that depends on the new network actually working.
- Keep the third objective as a fortify-and-stabilize objective so walls and spatial control matter.
- Extend the authored scenario family set later, but keep the same rule: the player must reconnect or stabilize a split logistics line rather than simply wait near the center.

### 5. AI Director & Policy Layer

**Current weakness**

- The earlier HW04 write-up treated the AI layer as a feature checklist item instead of a support layer for the core simulation.
- Group steering could distort local feasibility and produce misleading behavior.

**Why this misses the HW02 goal**

- The AI layer should explain and amplify system behavior, not replace the world simulation or invalidate it.

**Upgrade plan**

- Keep group policy as preference shaping, not direct control over local survival and delivery logic.
- Keep environment directives and policy outputs visible in the UI, but judge them by one standard: do they produce interpretable changes in traffic, roles, and pressure without breaking the base simulation?
- Continue using deterministic fallback as the reference baseline for regression, then compare live LLM behavior against that baseline.

### 6. Events / Weather / Ecology

**Current weakness**

- Weather and event pressure still act more like numeric turbulence than spatial gameplay in many runs.
- Predators, herbivores, traders, and saboteurs exist, but their relation to the player-built network is not always strong enough.

**Why this misses the HW02 goal**

- If events only move numbers, then they do not deepen the player's decisions about roads, storage, and defensive layout.

**Upgrade plan**

- Keep only the event and weather interactions that create readable map consequences.
- Keep localized weather hazard tiles so storms, rain, winter, and drought affect frontier routes and not just global multipliers.
- Keep spatial event payloads so bandit raids target concrete frontier zones, trade caravans reward reclaimed depot space, and animal migration pushes herbivores toward readable map destinations.
- Prefer threats that change route value, congestion, or safe build zones over purely scalar penalties.
- Tie walls and warehouse placement more directly to threat management so the player can see defensive geometry matter.

### 7. Interpretability

**Current weakness**

- The project has many debug panels, but not all of them help the player understand cause and effect.
- Some AI views report outputs that look authoritative even when the underlying behavior is weak.

**Why this misses the HW02 goal**

- The project promise is interpretable AI decisions. That means the debug layer has to help explain reroute and role changes, not just dump text.

**Upgrade plan**

- Keep telemetry that answers specific questions:
  - why did workers switch priorities?
  - why did routes change?
  - why did the objective stall?
- Keep the new explanation surfaces that answer those questions directly in moment-to-moment play:
  - HUD objective hints and frontier summaries
  - tile-level context in the Inspector
  - entity-level decision context in the Inspector and Entity Focus overlay
- Remove or deprioritize panels that do not help read those causal links.
- Treat the entity focus panel and AI trace as evidence tools, not decorative panels.

### 8. Performance / Deployment

**Current weakness**

- The build passes locally, but the previously published hosted URL was dead.
- The production bundle still emits a large chunk warning.

**Why this misses the HW02 goal**

- A system-heavy sandbox only matters if it can actually be run and verified by another person.

**Upgrade plan**

- Do not publish unverified hosted links in future reports.
- For this submission, treat the verified local build as the authoritative alpha artifact:

```bash
npm install
npm run dev -- --host 127.0.0.1 --port 4173
```

- Keep `npm test` and `npm run build` as mandatory validation gates for every milestone.
- If a new hosted deployment is added later, it must be checked on the same day it is reported.

## Weekly Development Plan

| Week | Focus | Deliverable |
|---|---|---|
| Week 9 | Fix Active overlay input blocking, restore right-drag camera control, protect worker `deliver` from AI override, verify local alpha playability. | A run that can be started, dragged, edited, and inspected without pointer failure. |
| Week 10 | Rebuild the default scenario and objective chain so the first milestone requires map editing and reroute-driven expansion. | A sparse, build-first opening scenario with logistics pressure. |
| Week 11 | Strengthen spatial consequences: warehouses, roads, walls, and chokepoints must create visible route and threat changes. | A more legible map-editing sandbox where infrastructure placement clearly matters. |
| Week 12 | Reduce noisy AI steering and keep only causal, readable policy behavior. Remove or downgrade steering that produces fake complexity. | A cleaner AI layer that supports the simulation instead of distorting it. |
| Week 13 | Balance the alpha, capture evidence, verify local build and any hosted build, and finalize release notes. | Submission-ready alpha evidence and stable report text. |
| Week 14 (Buffer) | Bug fixes, deployment checks, documentation, regression tests only. No new mechanics. | Submission freeze. |

## Release Notes Since HW03

### New

- Added a scenario-family authoring layer that maps the six terrain templates into three gameplay-relevant families:
  - `frontier_repair`
  - `gate_chokepoints`
  - `island_relay`
- Replaced the old one-size-fits-all objective flow with family-specific logistics repair objectives, then stockpile recovery, then fortify-and-stabilize.
- Added authored anchors, route links, depot zones, choke points, wildlife zones, and weather/event focus zones for each scenario family.
- Added explicit scenario-specific opening repairs before the colony can scale up:
  - reconnect the west route and reclaim the east depot
  - reopen the basin gates and secure the granary corridor
  - bridge the island relay and claim the relay depot
- Added spatial weather fronts and hazard tiles so route cost changes are localized and visible.
- Added spatial event payloads so bandit raids, trade caravans, and animal migration now target readable frontier zones.
- Added per-front weather severity data, including per-tile hazard penalties, front summaries, focus labels, and an aggregate `spatialPressure` runtime metric.
- Added pressure-scored event payloads, so raids and caravans now expose target severity, weather overlap, contested tiles, depot / route readiness, and reward or loss multipliers instead of only a target label.
- Added spatial-pressure summaries to `WorldSummary`, progression pacing, the Event Queue panel, and interpretation helpers so contested lanes now surface in both AI context and player-facing telemetry.
- Added a construction semantics layer so roads, worksites, warehouses, walls, and demolition now have explicit placement rules instead of acting like unrestricted tile paint.
- Added a live Construction panel plus tile-level build preview details in the Inspector, so the player can read cost, placement rules, scenario effects, and warnings before committing a placement.
- Added demolition salvage and build-history support for structure clearing, so editing the map is less punitive and easier to iterate on.
- Added live logistics summaries so HUD / Inspector can report carrier pressure, depot distance, overloaded warehouses, and stretched worksites instead of leaving bottlenecks hidden.
- Added carry-age delivery pressure and congestion-aware warehouse unloading so storage layout now affects throughput over time instead of only acting as a binary on/off requirement.
- Added a congestion-sampling layer on top of boids movement, so crowded corridors now publish traffic hotspots, lane pressure, and localized path-cost penalties instead of staying invisible.
- Added traffic-aware path versions and cache keys, so route reuse now respects hotspot changes instead of pretending all dynamic path cost stays constant while the grid is unchanged.
- Added traffic interpretation surfaces across the HUD, Inspector, Entity Focus panel, and Developer telemetry, so the player can see where crowding is happening and why reroutes should occur.
- Added doctrine-adjusted progression targets, so logistics, stockpile, and stability goals now scale by doctrine instead of using one static requirement set.
- Added an emergency recovery layer with limited charges, collapse-risk tracking, and a timed relief window, so the colony now has a map-dependent comeback path instead of only hard fail states.
- Added objective-aware worker role assignment, so doctrine and current objective deficits now influence the live farm vs. wood split instead of only changing abstract target numbers.
- Added progression telemetry to Developer Dock, including doctrine mastery, recovery charges, active recovery window, and collapse risk.
- Added scenario-aware visitor targeting so traders now prefer reclaimed depot lanes and saboteurs now prefer exposed frontier infrastructure instead of using a mostly nearest/random target rule.
- Added visitor-side defense interactions so wall coverage and sabotage resistance can now block or soften a sabotage run instead of walls only mattering to global threat math.
- Added visitor strategy interpretation so Entity Focus / Inspector can explain why a trader prefers a depot lane or why a saboteur is pressuring a particular target.
- Added wildlife-zone habitat metadata and scenario-aware animal spawning, so herbivores and predators now start from authored frontier habitats instead of scattering uniformly across the whole map.
- Added an ecology pressure layer that converts herbivore grazing into persistent frontier farm pressure, then feeds that pressure back into worker farm yield instead of leaving wildlife as background motion.
- Added predator hotspot patrol behavior, so predators now return to frontier habitats and pressured farm lanes when prey is absent instead of drifting randomly until a hunt begins.
- Added ecology interpretation in Developer Dock and `WorldExplain`, so farm pressure, wildlife-zone occupancy, predator patrol focus, and migration labels are visible during play.
- Clarified the in-game control contract so camera pan, build, and inspect no longer compete.
- Added interpretability summaries to the HUD, Inspector, and Entity Focus panels so players can see why a tile, route, or entity matters.
- Added explicit AI policy contracts, shared prompt payload builders, and guarded narrative fields so fallback and live-AI paths now speak the same structured language about focus, summary, steering notes, and allowed target priorities.
- Added scenario, objective, frontier, logistics, ecology, and recovery context to both environment-director and NPC-policy prompt payloads, so the AI layer now receives the same authored world structure that the rest of the simulation uses.
- Added policy-driven target selection for workers, traders, saboteurs, herbivores, and predators, so target priorities now change concrete target scoring instead of remaining mostly decorative metadata.
- Added richer AI explanation surfaces in the parsed decision panel and Entity Focus panel, including policy focus, policy summary, steering notes, target priorities, and live target-selection metrics.
- Added a shared causal-digest layer in `WorldExplain`, so HUD, Inspector, Entity Focus, the parsed AI panel, and the developer AI trace now read the same headline, warning focus, and evidence list instead of each panel inventing its own narrative.
- Added a `Live Causal Chain` block to the parsed AI panel, so the player can see the current headline, next move, warning focus, AI summary, and supporting evidence in one place instead of mentally stitching multiple panels together.
- Added `System Narrative` / `Why It Matters` sections to the Inspector, plus global headline / warning context inside Entity Focus, so tile, entity, and AI views now point at the same scenario blocker instead of drifting apart.
- Added warning filtering in the HUD so internal feasibility-drop telemetry stays in the developer dock while player-facing warning text stays focused on map, logistics, weather, and event pressure.
- Added global keyboard shortcuts for the core interaction loop: `1-6` build tools, `Esc` clear selection, `Space` pause/resume, and `Ctrl/Cmd+Z` undo/redo.
- Added snapshot camera persistence so saved views restore the same zoom and target instead of dropping the player into a different framing.
- Added a deterministic soak runner in `scripts/soak-sim.mjs` plus Stage 12 artifact paths at `docs/assignment4/metrics/soak-report.json` and `docs/assignment4/metrics/perf-baseline.csv`, so all three authored scenario families can be stress-run and benchmarked through the full simulation stack without opening the browser.
- Added a restored `scripts/verify-a3.mjs` chain plus `docs/assignment3/verification-summary.json`, so `verify:full` now closes over the stored HW03 live-AI / fallback proofs and the freshly generated performance baseline instead of failing on a missing script.
- Added Stage 12 bundle partitioning in `vite.config.js`, splitting `three` and `src/ui` into explicit chunks while keeping the final build under a verified `650 kB` warning gate.

### Fixes

- Fixed the Active-state overlay so it no longer blocks pointer events during gameplay.
- Restored right-drag camera panning and moved inspect-only behavior to `Alt + Left Click`.
- Fixed reset/regenerate camera framing so the view returns to a reliable default instead of inheriting stale pan state.
- Fixed worker delivery precedence so carry can flow back to warehouses instead of stalling in a harvest loop.
- Sanitized fallback and runtime state targets so workers and traders are no longer pinned into sticky local action states.
- Prevented group policy from forcing non-hungry workers into survival states.
- Fixed event activation timing so spatial side effects apply as soon as an event becomes active.
- Fixed event and weather path pressure so frontier hazards affect pathfinding instead of only changing global numbers.
- Fixed event targeting so bandit raids now prefer exposed, weather-stressed frontier lanes, and trade caravans now lose value when the depot corridor is storm-contested.
- Fixed snapshot restoration for newer weather-front and spatial-pressure fields, so older snapshots now backfill missing nested metrics instead of silently dropping Stage 9 state.
- Fixed the AI schema / guardrail gap where group policies could carry unsupported intent keys, unsupported target-priority keys, or missing explanation fields without being normalized back into the allowed contract.
- Fixed legacy visitor-policy normalization so split `traders` / `saboteurs` runtime policies are now pruned against their actual destination-group contracts instead of inheriting stale generic keys.
- Fixed the AI UI blank-state issue where environment directives could render with empty `focus`, `summary`, or `steeringNotes` text on partial fallback output; guarded defaults now ensure those fields stay legible.
- Fixed a runtime `AnimalAISystem` regression where predator targeting called `distanceSq` without a local definition, which could pollute the HUD with `AnimalAISystem failed: distanceSq is not defined` during live play.
- Fixed event/weather interpretation so `WorldExplain`, progression hints, and the Event Queue panel now report front labels, overlap, severity, and impact points instead of only raw type/status text.
- Invalidated tile caches after scenario rewrites so warehouse and structure counts match the actual grid.
- Removed hardcoded broken-frontier assumptions from objectives, weather focus, event targeting, and interpretability layers so those systems now read scenario metadata instead.
- Blocked construction from overwriting existing critical structures without demolition first.
- Fixed the broken full-verification path by restoring the missing `verify:a3` script and making the HW03 proof check offline-stable instead of depending on an external live-AI session during every `verify:full` run.
- Fixed snapshot roundtrip coverage gaps by adding repeated serialize / restore regression checks for nested Maps, Sets, weather fronts, ecology metadata, and AI policy state.
- Fixed the noisy Stage 12 build warning path by replacing the single oversized production bundle with explicit `vendor-three` and `ui` chunks instead of leaving Vite to emit one unstructured megachunk warning.
- Blocked isolated road painting, logistics-free worksite placement, and warehouse spam that collapses depot spacing.
- Fixed wall anchoring so out-of-bounds tiles no longer count as fake wall neighbors on map edges.
- Fixed starvation reachability so workers now consider both warehouse access and nearby farm fallback under the current hazard-aware path cost.
- Fixed fallback and policy target handling so workers and traders are no longer steered into sticky local action states that break delivery or survival logic.
- Fixed path reuse so congestion-sensitive reroutes are no longer hidden behind a grid-only path cache.
- Fixed worker and visitor path validity checks so dynamic traffic pressure can invalidate stale routes without mutating the map itself.
- Fixed HUD warning priority so low-severity runtime warnings no longer hide active traffic hotspots from the main player-facing status line.
- Fixed progression rewards so the final doctrine bonus now persists as doctrine mastery instead of being overwritten on the next update.
- Fixed stockpile pacing so high raw resources no longer complete the middle objective when prosperity or core frontier support is still unstable.
- Fixed recovery gating so emergency relief does not fire before the player has reopened at least part of the frontier support network.
- Fixed trader targeting so reclaimed depots and defended trade lanes can outscore the nearest core warehouse.
- Fixed saboteur targeting so exposed depot and frontier assets are now prioritized over equally valid but more defended core structures.
- Fixed sabotage resolution so walls and sabotage resistance can actually block an attack instead of only reducing abstract threat elsewhere in the simulation.
- Fixed animal habitat initialization so scenario wildlife zones now matter at spawn time instead of only appearing in authored metadata and explanation text.
- Fixed herbivore grazing so it now creates persistent farm pressure that survives across frames, decays over time, and can actually suppress worker output.
- Fixed predator idle behavior so frontier predators now patrol hotspot farms and home habitats when prey is absent instead of collapsing into meaningless roaming.
- Fixed animal migration interpretation so the herd now keeps the scenario label for its migration target, not just raw coordinates.

### Known Issues

- The current alpha is functional, but the scenario families still need stronger mid-game pressure after the first logistics buildout.
- The weather and event systems are now spatial, but they still need stronger mid-game escalation and more authored scenario families beyond the current three-family set.
- Six terrain templates now map into three authored scenario families. That is enough for Stage 2, but not yet a deep scenario library.
- Construction is now rule-based and scenario-aware, but farms and lumber sites still use coarse tile semantics rather than richer terrain-yield variation.
- Logistics summaries are intentionally lightweight and sampled; they expose bottlenecks well enough for alpha play, but they are not yet exact network-flow simulations.
- Traffic pressure is now visible and path-aware, but it is still sampled from local occupancy rather than a full reservation-based traffic model.
- Hotspot-driven path invalidation is intentionally conservative: it reacts to stable crowded lanes, not every small local shuffle, so some mild congestion will still only affect newly planned routes.
- Objective hints now prefer immediate network-coverage problems over abstract prosperity / stockpile gates when both are failing at once. This is usually the right call, but it can hide the higher-level pacing rule until the route issue is solved.
- Doctrine now changes worker farm-vs.-wood allocation through objective-aware biasing, but it still does not yet reshape visitor behavior, ecology pressure, or event strategy at the same level.
- Visitor pressure is now much more spatial, but it is still single-agent and target-centric. Traders do not yet form shared caravans, and saboteurs do not yet coordinate multi-step raids or retreat paths.
- Sabotage defense is now meaningful, but it is still a lightweight block-or-hit model rather than a richer combat / interception loop.
- Ecology pressure now affects farm yield and interpretation, but it does not yet create direct worker avoidance, crop destruction, or structure damage.
- Predators now patrol meaningful frontier zones, but they still threaten herbivores far more than workers, depots, or explicit defense loops.
- `vite build` still reports a large chunk warning, although the build completes successfully.
- I did not include the previous hosted link because it was unreachable on March 10, 2026, and I could not verify a reproducible deployment target from this repository alone.

## Known Risks / Validation Gates

Every future revision of the alpha should pass the following checks before it is reported as playable:

- Input regression: after `Start Run`, the overlay must be hidden, the viewport center must hit the `canvas`, left click must select a tile or entity, and right drag must move the camera target.
- Economy regression: workers with carry must return to `deliver`, and policy/fallback steering must not override that local requirement.
- Gameplay regression: the default seed must not pass the main objective chain without meaningful map edits.
- Progression regression: stockpile must stay blocked until both prosperity and frontier support are healthy, recovery must stay locked until route/depot support exists, and final doctrine mastery must persist once earned.
- Visitor regression: traders must prefer reclaimed depot lanes when those lanes are more valuable than the core warehouse, saboteurs must prefer exposed frontier assets over defended core ones, and defended sabotage targets must be blockable.
- Spatial pressure regression: weather fronts and active events must mark concrete frontier zones and change pathing or local behavior in those zones.
- Navigation regression: changing a congestion hotspot must invalidate affected paths and push new routes away from the hot lane when a viable alternative exists.
- Ecology regression: wildlife habitats must spawn against authored frontier zones, herbivore grazing must raise farm pressure and reduce harvest yield, predators must patrol hotspot farms or frontier habitats when prey is absent, and migration labels must remain visible in entity context.
- AI regression: policy and state targets may bias coordination, but they may not override survival, delivery, or feasibility.
- Interpretability regression: the current objective hint, frontier summary, tile context, and entity decision context must agree with the world state they are describing.
- Build regression: `npm test` and `npm run build` must pass before submission.

## Stage Log

### Stage 1. Input / Game Loop / Interaction Polish

**Goal**

- Make the start-active-end flow, build/select/inspect loop, shortcuts, reset behavior, and snapshot recovery stable enough that a new player can operate the alpha without hidden rules.

**Problems found before work**

- The active overlay had already been fixed not to block hit testing, but Stage 1 still lacked a complete interaction pass.
- Session state was not modeled as a first-class saved runtime object, so phase-sensitive features were harder to reason about and validate.
- The game had no global keyboard shortcut layer for the main interaction loop.
- Reset, regenerate, and snapshot recovery did not fully stabilize camera framing, so the player could be left in an inherited pan state after a restart or load.
- UI messaging still relied too heavily on raw text, with weak severity cues for success versus errors.

**Iteration A fixes**

- Added a serializable `session` runtime state (`menu`, `active`, `end`) and normalized legacy snapshots during restore.
- Replaced the old local run-phase bookkeeping in `GameApp` with `state.session`, so pause, benchmark, render locks, and end-state checks now agree on the same source of truth.
- Added a global shortcut resolver for build tools and session controls:
  - `1-6` for tools
  - `Esc` to clear selection
  - `Space` to pause/resume while active
  - `Ctrl/Cmd+Z` and `Ctrl/Cmd+Shift+Z` / `Ctrl/Cmd+Y` for undo and redo
- Tightened selection behavior so tile selection clears stale entity focus and entity selection clears stale tile focus.

**Iteration B expansions**

- Added stronger UI feedback by surfacing action severity (`info`, `success`, `error`) directly in the HUD.
- Updated the overlay and build toolbar hints so the current control contract is visible in the interface instead of only in report text.
- Added snapshot camera persistence, so a saved view restores the same zoom and target when the snapshot is loaded.
- Added deterministic camera resets on regenerate/reset so the player re-enters a centered, readable framing instead of inheriting an arbitrary previous pan.

**Iteration C tuning and debugging**

- Fixed residual OrbitControls damping leakage during regenerate/reset/load by flushing stale pan inertia before finalizing the restored view.
- Re-tested pause/resume and undo/redo while the simulation was actively running, not only in unit tests.
- Cleaned the overlay copy so shortcut hints render as actual inline code instead of raw backticks.

**Tests added or changed**

- Added `test/shortcut-resolver.test.js` for digit tools, pause gating, selection clear, and undo/redo shortcuts.
- Added `test/hud-controller.test.js` so the HUD now verifies session phase text and action severity styling hooks.
- Extended `test/snapshot-service.test.js` to confirm session state and saved camera view metadata survive snapshot serialize/restore.
- Kept `test/game-state-overlay.test.js` as the overlay hit-testing regression for active play.

**Manual validation**

- March 10, 2026 local browser validation on `http://127.0.0.1:4173/` confirmed:
  - after `Start Run`, the overlay is hidden and the viewport center hits the `canvas`
  - right-drag changes the camera target
  - left-click selects/builds and `Alt + Left Click` inspects without building
  - `1-6`, `Esc`, `Space`, and undo/redo shortcuts work in active play
  - clicking a worker selects the entity and clears tile selection
  - saving a snapshot, resetting to menu, and loading the snapshot restores both the active phase and the saved camera framing
- Additional input smoke checks were run on three scenario families:
  - `temperate_plains`
  - `fortified_basin`
  - `archipelago_isles`

**Remaining risks**

- Small sub-tile camera drift can still appear immediately after a regenerate if the previous drag ended on the same frame; the reset is now stable enough for play, but this camera path could still be tightened further later.
- Stage 1 is stable, but it does not solve the deeper scenario-authoring and progression problems. Those remain Stage 2 and Stage 6 work.

### Stage 2. Map Generation / Terrain Authoring / Scenario Anchors

**Goal**

- Convert the terrain templates into authored scenario families with distinct topology, objective anchors, and map-readable pressure zones so Stage 1's polished controls actually operate on meaningful spaces.

**Problems found before work**

- The alpha still behaved like a single broken-frontier opener with slight terrain variance rather than a real scenario library.
- Objective logic, weather focus, event targeting, and interpretability still depended on hardcoded west/east assumptions from the old layout.
- Several templates looked different visually but did not create materially different strategic problems.
- Scenario metadata was too thin to drive downstream systems consistently.

**Iteration A fixes**

- Added a dedicated scenario authoring layer in `src/world/scenarios/ScenarioFactory.js`.
- Mapped all six terrain templates into three authored families:
  - `temperate_plains`, `fertile_riverlands` -> `frontier_repair`
  - `rugged_highlands`, `fortified_basin` -> `gate_chokepoints`
  - `archipelago_isles`, `coastal_ocean` -> `island_relay`
- Moved scenario layout generation out of `EntityFactory` and into family builders that now return structured metadata for:
  - anchors
  - route links
  - depot zones
  - choke points
  - wildlife zones
  - weather focus
  - event focus
  - scenario-specific targets and objective copy
- Rewired initial state creation so gameplay now stores scenario metadata directly and objectives are created from the scenario bundle instead of from one hardcoded opener.

**Iteration B expansions**

- Authored three distinct family identities instead of simple terrain skins:
  - `frontier_repair` keeps the west lumber line / east depot reclaim pattern
  - `gate_chokepoints` creates a fortified basin with blocked gate corridors and a south granary recovery path
  - `island_relay` creates water-split logistics with causeway lanes, a harbor, and a relay depot
- Generalized `ProgressionSystem` so logistics, stockpile, and stability targets all resolve from scenario metadata rather than static broken-frontier numbers.
- Generalized `WeatherSystem` so hazard placement follows per-scenario focus zones instead of relying on one layout.
- Generalized `WorldEventSystem` so raids, caravans, and animal migration pick scenario event candidates rather than old fixed frontier assumptions.
- Generalized `WorldExplain` and the Inspector / Entity Focus panels so route status, depot status, tile context, and decision context all read the current scenario family.

**Iteration C tuning and debugging**

- Tuned each family so the opening map is sparse but legible:
  - `frontier_repair` remains the clearest default introduction
  - `gate_chokepoints` emphasizes corridor defense and gate reopening
  - `island_relay` emphasizes fragmented navigation and depot bridging
- Re-tested objective progress against actual grid state instead of only synthetic building counters, which exposed and fixed count mismatches after scenario rewrites.
- Re-ran input validation across all three family types to confirm that Stage 1 controls still work after Stage 2 terrain authoring changes.
- Verified that weather and event focus zones now resolve on multiple templates instead of silently falling back to the old frontier layout.

**Tests added or changed**

- Added `test/scenario-family.test.js` for family mapping and scenario focus metadata.
- Updated `test/alpha-scenario.test.js` so the existing alpha opener now validates the `frontier_repair` family structure.
- Updated `test/progression-system.test.js` so objective progress reads actual scenario route gaps and grid counts.
- Added or updated:
  - `test/weather-spatial.test.js`
  - `test/world-event-spatial.test.js`
  - `test/world-explain.test.js`
- Full regression status after Stage 2:
  - `npm test` -> `77/77` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed three materially different authored starts:
  - `temperate_plains` -> `frontier_repair` / `Broken Frontier`
  - `fortified_basin` -> `gate_chokepoints` / `Gate Bastion`
  - `archipelago_isles` -> `island_relay` / `Island Relay`
- In all three cases:
  - the active overlay stayed hidden
  - viewport center hit the `canvas`
  - right-drag changed the camera target
  - the HUD and objective hint updated to the correct scenario family

**Remaining risks**

- Stage 2 now provides three strong scenario families, but mid-game pacing inside each family still needs deeper escalation and comeback structure.
- Only three authored families exist today; the six terrain templates are still grouped into those three higher-level patterns rather than into six fully distinct authored campaigns.
- Small residual camera drift can still appear immediately after regenerate on some templates, although interaction remains stable enough for play.

### Stage 3. Construction / Building Semantics / Editing UX

**Goal**

- Turn building from unrestricted tile painting into a real construction layer with readable placement rules, scenario-aware consequences, and lower-friction editing recovery.

**Problems found before work**

- Buildings could overwrite each other far too freely, so structure identity was weak and accidental invalid states were easy to create.
- Roads could be painted in isolation, which broke the core idea that map edits should reshape an actual logistics network.
- Farms, lumber sites, warehouses, and walls had almost no placement semantics beyond "not water" and "can afford."
- The UI showed static costs, but not why a placement was valid, invalid, strategically useful, or dangerous.
- Erasing structures gave no salvage feedback, which made iteration clumsy even after undo/redo existed.

**Iteration A fixes**

- Added `BuildAdvisor` as a dedicated construction semantics layer instead of keeping all placement logic inside a minimal `BuildSystem`.
- Introduced explicit per-tool placement rules:
  - roads must extend existing infrastructure or repair a scenario route gap
  - farms and lumber sites need nearby road / warehouse access
  - warehouses need road access and must be spaced apart unless reclaiming a depot zone
  - walls must extend from a real defense anchor or a scenario chokepoint
- Blocked structure overwrite for critical tiles, so roads, farms, warehouses, and walls now require demolition or a valid replacement case instead of silently flattening each other.
- Added richer failure reasons such as `occupiedTile`, `needsNetworkAnchor`, `needsLogisticsAccess`, `needsRoadAccess`, `warehouseTooClose`, and `needsFortificationAnchor`.

**Iteration B expansions**

- Added scenario-aware preview effects:
  - roads on authored gap tiles now report that they advance the relevant route repair
  - warehouses in depot zones now report that they reclaim the relevant depot
  - walls in chokepoints now report that they fortify the relevant gate or corridor
- Added contextual warnings for exposed placements, including wildlife zones and active weather hazard zones.
- Added a live Construction panel that now reports:
  - selected tool identity
  - tool cost
  - placement rules
  - current hover / preview outcome
- Added tile-level Build Preview detail to the Inspector so the player can read validity, cost, salvage, effects, and warnings on the currently inspected tile.
- Added demolition salvage, so erasing built structures now returns part of their original cost and makes long editing sessions less punishing.

**Iteration C tuning and debugging**

- Fixed a real wall-anchoring bug found in browser validation: out-of-bounds tiles were being treated as fake wall neighbors because `getTile()` returns `WALL` outside the grid.
- Reworked build tests so they validate legal anchors and spacing rules instead of assuming the old free-paint behavior.
- Verified that undo / redo still behaves correctly with salvage-aware demolition.
- Re-tested the Construction panel and Inspector preview so they stay in sync with the currently selected tool and scenario context.

**Tests added or changed**

- Expanded `test/build-system.test.js` to cover:
  - structure overwrite blocking
  - isolated road rejection
  - worksite logistics access rules
  - warehouse spacing rules
  - demolition salvage with undo / redo
  - scenario-aware preview effects for roads, warehouses, and walls
- Full regression status after Stage 3:
  - `npm test` -> `81/81` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed:
  - the Construction panel updates when the selected tool changes
  - a warehouse preview in the east depot zone reports reclaim progress and weather warnings when relevant
  - a road preview on the west route gap reports route-repair progress
  - an isolated road preview reports the new network-anchor failure reason instead of allowing free painting
  - demolition returns salvage and visibly changes resources
- Additional manual checks confirmed that the first valid wall preview no longer appears on arbitrary map-edge tiles after the anchor fix.

**Remaining risks**

- Construction semantics are now much stronger, but they still operate on coarse tile classes rather than richer terrain fertility / timber quality layers.
- Walls now require real anchors, but the game still lacks stronger downstream threat behaviors that would fully capitalize on defensive geometry.
- The Construction panel currently reads preview state well, but it does not yet visualize projected throughput deltas or queue impact from a placement.

### Stage 4. Worker Economy / Logistics / Warehouse Throughput

**Goal**

- Make the economy survive on system design instead of lucky local loops by tightening delivery correctness, introducing visible warehouse throughput pressure, and surfacing bottlenecks directly in the UI.

**Problems found before work**

- Delivery correctness still depended on multiple fragile layers staying aligned: local planner rules, fallback targets, policy targets, and worker state transitions.
- Warehouses still behaved mostly as binary flags. Once one existed, the simulation did not strongly communicate when one depot was overloaded or when a second depot would materially help.
- Workers could still accumulate low amounts of carry for too long before returning, which made some routes look active but low-throughput.
- Starvation / recovery logic needed to reflect actual reachable nutrition under current path cost, not just raw global food counts.
- The UI reported objectives and weather well, but it still did not make logistics bottlenecks legible enough during live play.

**Iteration A fixes**

- Folded in the protected local-state fixes for worker delivery and survival so `deliver` remains shielded from weaker AI / fallback steering.
- Folded in fallback target sanitization so worker and trader state targets no longer resolve to sticky local states such as `harvest`, `trade`, `eat`, or `seek_food`.
- Folded in starvation reachability improvements so mortality checks now consider hazard-aware path cost to warehouses plus nearby farm fallback instead of relying on raw global resource counts alone.
- Added regression coverage for these correctness cases in:
  - `test/state-planner-policy.test.js`
  - `test/policy-fallback-state-template.test.js`
  - `test/mortality-system.test.js`

**Iteration B expansions**

- Added live logistics metrics to `ResourceSystem`, including:
  - carrying workers
  - carry in transit
  - average depot distance
  - overloaded warehouses
  - stretched / isolated worksites
  - per-warehouse inbound worker load
- Added carry-age delivery pressure to workers, so low-carry but long-delayed workers no longer stay in weak harvest loops forever.
- Added congestion-aware warehouse unloading, so a heavily targeted depot now unloads gradually instead of instantly, making multi-depot layouts more meaningful.
- Added logistics summary surfaces to the HUD warning line and the Inspector idle view so the player can see when storage coverage or depot load has become the real bottleneck.
- Extended entity insight so workers can now explain:
  - when carry pressure has been building for too long
  - when the target warehouse is congested

**Iteration C tuning and debugging**

- Tuned the new logistics parameters in `balance.js` instead of leaving them inline:
  - carry pressure timing
  - far-depot threshold
  - unload rate
  - queue penalty
  - worksite coverage radii
- Fixed a regression in lightweight worker-intent tests where missing `grid/x/z` data incorrectly triggered far-depot delivery pressure.
- Re-tested the HUD / Inspector wiring so logistics summaries appear only when there is no higher-priority runtime warning.
- Re-ran browser validation with forced logistics pressure to confirm that the summary text shown in the HUD matches the Inspector's logistics line.

**Tests added or changed**

- Added `test/resource-system-logistics.test.js` for live logistics metrics and warehouse load reporting.
- Added `test/worker-delivery-throughput.test.js` for congestion-aware partial unloading.
- Extended `test/worker-intent.test.js` with carry-age delivery pressure.
- Extended `test/hud-controller.test.js` so the HUD now verifies logistics summary visibility.
- Full regression status after Stage 4:
  - `npm test` -> `84/84` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed:
  - the HUD warning line can display a live logistics bottleneck summary
  - the Inspector idle view now includes a dedicated Logistics line alongside frontier, weather, and event context
  - a forced warehouse congestion state appears in worker-facing logic and summary text instead of remaining invisible
  - the run still starts cleanly and the Stage 1 / Stage 2 interaction loop remains intact while these logistics summaries are active

**Remaining risks**

- Warehouse throughput is now visible and partially simulated, but it is still based on soft queue pressure rather than explicit per-depot inventory buffers or worker line formation.
- Carry-age pressure reduces fake harvesting loops, but role assignment and doctrine still need deeper integration with logistics stress later.
- The current logistics summary is very useful for alpha debugging, but it still abstracts over exact route quality, weather duration overlap, and downstream visitor interference.

### Stage 5. Navigation / Reroute / Congestion / Boids Stability

**Goal**

- Make pathfinding react to real crowd pressure instead of only map edits and weather, then expose that pressure clearly enough that reroutes are readable in both player-facing UI and developer telemetry.

**Problems found before work**

- A* already handled grid changes and localized weather hazard cost, but it still treated traffic as invisible.
- Path reuse and path validity were keyed only to `grid.version`, so any congestion-aware reroute logic would have been silently suppressed by the existing cache and stale-path checks.
- Boids movement created crowd behavior visually, but that crowding did not become a first-class simulation signal.
- The UI could explain weather, events, and logistics, but it still could not answer a basic Stage 5 question: "which lane is clogged, and is my current path crossing it?"
- Low-severity runtime warnings could still crowd out more important path-pressure information in the HUD.

**Iteration A fixes**

- Extended navigation state so paths now carry both a grid version and a traffic version, instead of assuming that only tile edits can invalidate a route.
- Extended `PathCache` keys with a dynamic cost version, so cached paths no longer ignore congestion-aware reroute updates.
- Extended A* cost evaluation to accept traffic penalties in addition to weather hazard penalties.
- Replaced worker and visitor local `hasActivePath` checks with the shared navigation helper so stale congestion-aware paths are invalidated consistently instead of only in one subsystem.
- Added direct regression coverage for:
  - congestion-aware A* reroutes
  - `setTargetAndPath()` recomputing when only the traffic version changes

**Iteration B expansions**

- Added a sampled traffic layer inside `BoidsSystem`:
  - per-tile lane load
  - hotspot count
  - peak lane load
  - localized traffic path-cost penalties
  - a stable hotspot summary string
- Added tunable traffic parameters in `balance.js` instead of hardcoding them:
  - sampling interval
  - soft lane-load threshold
  - hotspot threshold
  - penalty-per-load
  - neighbor spill ratio
  - max penalty multiplier
  - crowd weights by entity family
- Added traffic summaries and traffic context to the major explanation surfaces:
  - HUD warning line
  - Inspector idle tile summary
  - tile context in the Inspector
  - entity decision context in the Inspector and Entity Focus panel
  - Developer telemetry algorithm panel

**Iteration C tuning and debugging**

- Tuned hotspot versioning so path invalidation reacts to stable hotspot structure, not every tiny fluctuation in sampled lane load. This prevents congestion-aware rerouting from collapsing into path thrashing.
- Added a second traffic UX pass so low-severity `warn` events no longer suppress a real hotspot summary in the HUD.
- Reworked the congestion regression tests so they use a credible alternate road corridor instead of relying on an unrealistically small detour.
- Re-ran Stage 5 browser validation in three scenario contexts by forcing the same hotspot into each authored family:
  - `Broken Frontier`
  - `Gate Bastion`
  - `Island Relay`

**Tests added or changed**

- Added `test/boids-traffic.test.js` for sampled hotspot metrics and stable hotspot versioning.
- Extended `test/astar.test.js` with congestion-penalty reroute coverage.
- Extended `test/navigation-repath.test.js` so same-target paths now recompute on traffic-version changes.
- Extended `test/world-explain.test.js` so tile and entity insights now mention traffic hotspots and congested routes.
- Extended `test/hud-controller.test.js` so HUD warning priority prefers active traffic hotspots over low-severity warn-only messages.
- Full regression status after Stage 5:
  - `npm test` -> `89/89` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed Stage 5 behavior across:
  - `temperate_plains` / `Broken Frontier`
  - `fortified_basin` / `Gate Bastion`
  - `archipelago_isles` / `Island Relay`
- For deterministic validation, I injected a controlled hotspot onto each scenario's opening route gap so the UI could be checked against the same crowd-pressure case.
- In all three cases:
  - the HUD warning line showed the traffic hotspot summary even when a low-severity AI warning existed
  - the Inspector tile context reported both the scenario objective meaning of the tile and the traffic hotspot cost
  - the selected worker's decision context explained that its current route crossed a congested lane
  - the Developer telemetry algorithm panel exposed the same hotspot in the Traffic subsection

**Remaining risks**

- The traffic model is intentionally lightweight and sampled. It is strong enough for alpha reroute readability, but it is not yet a full lane reservation or queueing simulation.
- Animals now use the shared scenario-aware ecology layer for farm pressure and frontier patrols, but they still do not participate in the same fully traffic-shaped congestion decisions as workers and visitors.
- Congestion is now legible and path-aware, but the game still needs stronger objective and doctrine coupling so the player is forced to care about these lanes more often during long runs.

### Stage 6. Objectives / Doctrine / Pacing / Failure-Recovery

**Goal**

- Turn progression into a real pacing system instead of a thin objective checklist by making doctrine materially affect targets and worker behavior, blocking stockpile progress behind actual stability, and adding a limited comeback path before total collapse.

**Problems found before work**

- Doctrine still looked more important on paper than in play. Outside of `farmBias`, it mainly changed passive numbers without reshaping the colony's actual development rhythm.
- The middle stockpile objective could still collapse into a raw resource check once logistics were repaired, which meant prosperity and frontier support were not yet acting like real gates.
- The failure side of the loop was still too binary: either the colony survived on its own or it jumped straight to run-ending conditions with very little structured recovery space.
- The final objective reward text promised a permanent doctrine bonus, but the implementation rewrote doctrine modifiers every update, so that reward was immediately lost.

**Iteration A fixes**

- Moved Stage 6 pacing constants out of inline literals and into `balance.js`, including:
  - recovery cooldown / duration / charge cap
  - role-bias clamps and blending weight
  - stability hold decay
  - doctrine mastery reward multiplier
- Added first-class `gameplay.recovery` and `gameplay.doctrineMastery` state so recovery and late-run doctrine tuning are explicit runtime concepts instead of hidden temporary values.
- Reworked `ProgressionSystem` so doctrine-adjusted targets are computed from the current scenario runtime, not from one hardcoded baseline.
- Fixed the final reward bug by replacing the old modifier-multiplication hack with persistent doctrine mastery that survives later updates.

**Iteration B expansions**

- Extended progression targets so doctrine now changes:
  - logistics buildout targets
  - stockpile requirements
  - stability thresholds
  - recovery package strength
- Extended the stockpile objective so it now requires both:
  - enough food / wood
  - enough prosperity and frontier support
- Added collapse-risk tracking and timed emergency relief:
  - recovery charges are limited
  - relief only unlocks after some route / depot support exists
  - relief grants a short rebuild window instead of permanently erasing failure pressure
- Extended `RoleAssignmentSystem` so current objective deficits now bias the live farm / wood split:
  - stockpile food pressure pushes more workers toward farms
  - industry logistics pressure pushes more workers toward lumber
  - stability pressure can pull the colony back toward wood for wall recovery
- Added progression telemetry to Developer Dock so the current mastery, recovery charges, active recovery window, and collapse risk are visible during play.

**Iteration C tuning and debugging**

- Fixed a real Stage 6 regression introduced during the rewrite: recovery charge clamping still referenced a deleted constant.
- Reworked doctrine-aware progression tests so they no longer assume the balanced doctrine target counts for every scenario.
- Reworked the late-stage stability fixture so defensive walls are placed on the outer edge of the map instead of accidentally blocking the colony's repaired main route.
- Ran deterministic browser validation with injected stage states to verify the progression layer itself instead of conflating it with unrelated live-sim noise:
  - `fortified_basin` / `Fortress Doctrine`: recovery stayed locked before depot support existed, then fired once the south granary was reclaimed
  - `archipelago_isles` / `Industrial Guild`: worker roles settled into a wood-heavy split (`FARM=6`, `WOOD=12`) during the logistics opener
  - `temperate_plains` / `Mercantile League`: injected late-stage completion persisted `doctrineMastery=1.08`, and the run advanced cleanly to victory

**Tests added or changed**

- Expanded `test/progression-system.test.js` to cover:
  - recovery charge refill on logistics completion
  - stockpile gating on prosperity plus frontier support
  - recovery staying locked before meaningful map repair
  - doctrine mastery persistence after final completion
- Added `test/role-assignment-system.test.js` for objective-aware worker allocation under stockpile pressure and industry logistics pressure.
- Full regression status after Stage 6:
  - `npm test` -> `94/94` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed three Stage 6 behaviors across the three authored scenario families:
  - `fortified_basin` / `Gate Bastion`: Developer Dock showed recovery `risk=76%` with `charges=1` before repair, then `recovery=0 active≈15s reason=resource collapse` after reclaiming the south granary depot, while the HUD action line reported emergency relief
  - `archipelago_isles` / `Island Relay`: the run stayed in the logistics opener, the objective hint stayed map-specific, and the worker population settled into a wood-heavy split under `industry`
  - `temperate_plains` / `Broken Frontier`: an injected end-stage state completed cleanly under `trade`, the run entered victory, and Developer Dock reported `mastery=1.08`
- I also kept the new automated regression cases for prosperity / network stockpile gating because those state transitions are easier to validate deterministically in tests than through freehand browser play.

**Remaining risks**

- Recovery is intentionally lightweight. It gives the player a rebuild window, but it is not yet a full multi-step comeback system with staged aid, debt, or lasting consequences.
- Objective hints now correctly prioritize route coverage problems when the network is unstable, but that means prosperity-only pacing gates may be less visible until the route issue is solved.
- Doctrine now affects worker allocation and progression thresholds, but it still needs stronger downstream influence on visitors, events, and ecology in later stages.

### Stage 7. Visitors / Trade / Sabotage / Frontier Pressure

**Goal**

- Turn traders and saboteurs into real spatial actors that respond to infrastructure quality, reward strong frontier logistics, and punish exposed layouts instead of acting like thin ambient NPCs.

**Problems found before work**

- Traders mostly behaved like nearest-warehouse seekers, so the player got very little extra value from reclaiming a meaningful depot lane versus idling around the core warehouse.
- Saboteurs mostly picked from global valid tiles, which meant exposed frontier assets and defended core assets were not separated strongly enough.
- Walls mattered to global threat and some event routing, but they still did not reliably change the outcome of a sabotage attempt itself.
- Visitor-facing interpretation was still too generic: the UI could tell me a saboteur existed, but not why it preferred one target over another.

**Iteration A fixes**

- Reworked `VisitorAISystem` target selection so traders and saboteurs now score candidates instead of using a mostly nearest/random rule.
- Added scenario-aware target context using existing authored metadata:
  - traders read `tradeCaravan` depot focus zones
  - saboteurs read `banditRaid` frontier focus zones plus depot pressure
- Added tunable visitor-pressure balance values to `balance.js` for:
  - trade lane bonus
  - route-support bonus
  - wall / road contribution
  - sabotage frontier / depot bonuses
  - sabotage wall penalties and block chance

**Iteration B expansions**

- Traders now prefer reclaimed depot lanes when they are defended, road-supported, and route-supported, and their live trade yield now scales with that lane quality instead of staying flat everywhere.
- Saboteurs now prefer exposed frontier assets, especially warehouses and depot-linked targets, instead of treating every valid sabotage target as equivalent.
- Added direct defense interaction on sabotage resolution:
  - nearby walls increase block chance
  - higher `sabotageResistance` improves the chance that a defended target survives
  - blocked sabotage now records an explicit event payload instead of silently doing nothing
- Added visitor decision context to `WorldExplain`, so selected traders and saboteurs now explain:
  - which lane or target they are prioritizing
  - how much wall coverage exists there
  - whether the last sabotage run was blocked

**Iteration C tuning and debugging**

- Fixed a real Stage 7 test-design issue: over-defending the core warehouse with four walls made it fully unreachable, which hid target-selection behavior behind path failure instead of testing exposed-vs-defended choice properly.
- Reworked visitor fixtures so depot targets are reachable before asking traders or saboteurs to choose them.
- Re-ran browser validation with deterministic state injection across all three authored scenario families:
  - `temperate_plains`: trader preferred the `east ruined depot` with a measured trade bonus of `x1.81`
  - `fortified_basin`: saboteur targeted the `south granary` instead of the more defended core
  - `archipelago_isles`: saboteur targeted the `central relay depot`, confirming depot pressure works on fragmented maps too

**Tests added or changed**

- Added `test/visitor-pressure.test.js` to cover:
  - trader preference for reclaimed depots
  - saboteur preference for exposed depot/frontier targets
  - sabotage blocking from walls plus sabotage resistance
- Updated `test/visitor-eating.test.js` so the trade regression now validates the stronger visitor economy model without assuming every warehouse produces the same trade output.
- Extended visitor interpretation coverage indirectly through `test/world-explain.test.js`, which now also exercises the new visitor insight paths.
- Full regression status after Stage 7:
  - `npm test` -> `97/97` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed three scenario-specific visitor outcomes:
  - `temperate_plains` / `Broken Frontier`: an injected trader chose the `east ruined depot` over the core warehouse once the lane was connected, with `tradeTargetBonus=1.81`
  - `fortified_basin` / `Gate Bastion`: an injected saboteur chose the `south granary`, showing that exposed basin infrastructure now out-scores the defended core
  - `archipelago_isles` / `Island Relay`: an injected saboteur targeted the `central relay depot`, showing that fragmented-water maps now create meaningful depot pressure instead of collapsing to random target choice

**Remaining risks**

- Trader reward is now lane-aware, but it is still local and continuous. There is no explicit caravan arrival, escort, or shared depot queue yet.
- Saboteurs now choose more credible targets and can be blocked by defenses, but they still act as single infiltrators rather than coordinated raid groups with retreat behavior.
- Visitor pressure is stronger and more spatial, but it still needs tighter coupling with future event escalation and ecology work.

### Stage 8. Animals / Ecology / Farm Pressure / Predator-Prey Dynamics

**Goal**

- Turn wildlife from ambient background motion into a real frontier-pressure layer by tying animal habitats to authored scenario zones, converting herbivore grazing into meaningful farm pressure, and making predators patrol the same frontier hotspots that now matter to the player.

**Problems found before work**

- Animal spawns still ignored authored wildlife zones, so the scenario metadata could describe a frontier habitat while the actual herds and predators started somewhere else entirely.
- Herbivores could graze and flee, but that behavior mostly only changed their own hunger. It did not yet feed back into the player's farm layout, stockpile loop, or map-editing decisions.
- Predators could hunt nearby herbivores, but when prey pressure was low they mostly collapsed back into generic roaming rather than reinforcing a readable frontier territory.
- Ecology-related interpretation was still too thin: migration targets existed, but there was no stable explanation for habitat occupancy, farm pressure, or predator patrol focus.

**Iteration A fixes**

- Added first-class animal habitat metadata in `EntityFactory`, including:
  - `homeZoneId`
  - `homeZoneLabel`
  - `homeTile`
  - `territoryAnchor`
  - `territoryRadius`
- Reworked initial animal spawning so herbivores and predators now seed near authored `wildlifeZones` instead of drawing uniformly from any valid map tile.
- Added an `ecology` metrics block to runtime state so farm pressure, wildlife-zone occupancy, predator frontier presence, and migration counts exist as explicit simulation outputs rather than temporary local variables.
- Fixed animal migration bookkeeping so a migrating herd now keeps both a spatial `migrationTarget` and the scenario label that produced it.

**Iteration B expansions**

- Reworked herbivore graze targeting so herds now prefer nearby frontier habitat and viable farms instead of only sampling a random nearby grass tile.
- Added persistent farm-pressure accumulation from herbivore grazing:
  - pressure builds while herbivores sit on a farm
  - pressure decays over time when the herd leaves
  - the ecology layer publishes hotspot farms and a readable summary string
- Reworked worker harvesting so farm output is now reduced by live ecology pressure on the target tile instead of treating every farm as equally productive forever.
- Reworked predator fallback movement so predators now patrol:
  - pressured farm hotspots
  - authored wildlife habitats
  - frontier territory when prey is absent
- Extended `WorldExplain` and Developer Dock so the game can now describe:
  - current wildlife-zone occupancy
  - active farm-pressure suppression
  - predator patrol focus
  - migration labels and frontier habitat ties

**Iteration C tuning and debugging**

- Tuned the new ecology values in `balance.js` instead of leaving them inline:
  - graze search radius
  - farm-attraction bias
  - home-zone bias
  - core avoidance penalty
  - farm-pressure build / decay rates
  - max farm-yield penalty
  - predator patrol timing and hotspot attraction
- Fixed a Stage 8 regression in the first test pass: animal FSMs, like the other unit systems, require a settled state before asserting graze / patrol behavior, so the ecology regressions were rewritten around stable post-transition updates instead of false one-tick failures.
- Re-ran the ecology checks in a real browser with deterministic state injection across the three scenario families:
  - `temperate_plains` / `Broken Frontier`: forcing a frontier farm at `westWilds` produced `farmPressure=0.312`, and a worker harvesting that farm reported `lastFarmYieldMultiplier=0.8627`
  - `fortified_basin` / `Gate Bastion`: a predator with no prey patrolled the pressured frontier farm at `(45, 30)` with `patrolLabel=farm-pressure hotspot`
  - `archipelago_isles` / `Island Relay`: a forced `animalMigration` event set `migrationLabel=north islet wilds` with `migrationTarget=(49, 22)`, and ecology occupancy reported the herd in `north-islet`

**Tests added or changed**

- Added `test/animal-ecology.test.js` to cover:
  - wildlife-zone-based animal spawning
  - herbivore farm pressure reducing worker harvest yield
  - predator patrol focus on frontier farm hotspots
- Extended `test/world-explain.test.js` so tile and entity interpretation now verify ecology pressure, wildlife-zone counts, and predator patrol text.
- Kept `test/world-event-spatial.test.js` as the migration-event regression and extended the implementation so migration labels survive into entity insight.
- Full regression status after Stage 8:
  - `npm test` -> `101/101` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed Stage 8 behavior across:
  - `temperate_plains` / `Broken Frontier`
  - `fortified_basin` / `Gate Bastion`
  - `archipelago_isles` / `Island Relay`
- In those deterministic checks:
  - frontier farms now accumulate ecology pressure instead of acting like permanently free food nodes
  - pressured farms now feed through to worker harvest yield multipliers
  - predators now patrol frontier hotspots instead of idling into meaningless random roam
  - migration events now preserve both a target tile and a readable frontier-zone label

**Remaining risks**

- Ecology pressure now changes farm throughput and explanation text, but it still does not yet create richer outcomes such as crop destruction, worker fear, or evacuation from frontier farms.
- Predator patrol now reinforces frontier territory, but predators still pressure animals much more than they pressure explicit colony defense loops.
- Wildlife zones now shape spawning and patrol, but later stages still need stronger ties between ecology pressure, doctrine, visitors, and mid-game event escalation.

### Stage 9. Weather / Events / Spatial Escalation

**Goal**

- Turn weather and events from localized labels into a real spatial-pressure layer that changes route cost, raid severity, caravan value, pacing hints, and player-readable frontier consequences.

**Problems found before work**

- Weather already spawned localized hazard tiles, but pathing still treated those zones mostly through one coarse global penalty and one generic label.
- Events already targeted scenario zones, but the payloads did not expose severity, overlap, depot readiness, route readiness, or contested-lane pressure.
- The progression layer still reacted to weather/events as flat penalties, so objective pacing could ignore whether pressure was concentrated on a specific route, depot, or choke.
- The Event Queue panel and interpretation layer were still too thin: they could report type/status, but not why one frontier lane was dangerous or why a caravan had become low-value.
- Snapshot compatibility was only partially updated for the new Stage 9 data, so older saved states could keep a `spatialPressure` object while still missing the nested fields Stage 9 depends on.

**Iteration A fixes**

- Finished the weather-front data model in `WeatherSystem`:
  - per-tile `hazardPenaltyByKey`
  - per-tile hazard labels
  - explicit `hazardFronts`
  - a readable `hazardFocusSummary`
  - aggregate weather `pressureScore`
- Extended `AStar` and `Navigation` so pathfinding now reads per-tile weather penalties instead of only falling back to one global weather multiplier.
- Finished Stage 9 snapshot/state plumbing so runtime state now initializes and restores:
  - `hazardPenaltyByKey`
  - `hazardLabelByKey`
  - `hazardFronts`
  - `hazardFocusSummary`
  - `pressureScore`
  - `metrics.spatialPressure`
- Added nested default backfills for older snapshots so partially updated `spatialPressure` objects no longer miss new Stage 9 fields after restore.

**Iteration B expansions**

- Reworked `WorldEventSystem` so bandit raids, trade caravans, and animal migration now score scenario focus zones against:
  - wall coverage
  - road support
  - warehouse / depot support
  - route readiness
  - wildlife occupancy
  - weather overlap
- Expanded live event payloads so the simulation now carries explicit spatial metadata:
  - `pressure`
  - `severity`
  - `hazardOverlapTiles`
  - `contestedTiles`
  - `routeOnline`
  - `depotReady`
  - `rewardMultiplier`
  - `lossMultiplier`
  - `impactTile`
  - `secondaryImpactTile` for high-pressure raids
- Added aggregate `metrics.spatialPressure`, so the colony now tracks:
  - weather pressure
  - event pressure
  - contested zones / tiles
  - active event count
  - peak event severity
- Reworked `ProgressionSystem` so prosperity, threat, and pacing hints now read the new spatial-pressure metrics instead of only using flat weather/event counts.
- Reworked `WorldSummary` so the AI layer now receives weather-front and event-pressure context instead of only bare event types.
- Expanded `WorldExplain`, `EventPanel`, and Developer Dock so the player can now read:
  - which fronts are active
  - which zone an event is pressuring
  - how much weather overlap exists
  - whether a lane is contested
  - whether a caravan is still worth relying on

**Iteration C tuning and debugging**

- Tuned all new Stage 9 constants in `balance.js` instead of leaving inline numbers inside the event system:
  - weather pressure prosperity / threat weights
  - event pressure prosperity / threat weights
  - contested-zone penalties
  - raid pressure bonuses and secondary-impact threshold
  - caravan support bonuses and hazard penalties
  - migration pressure scaling
- Fixed the main compatibility bug found during Stage 9 validation: snapshots that already had a `spatialPressure` object but lacked the new nested Stage 9 fields now backfill each field individually on restore.
- Re-ran deterministic browser validation on March 10, 2026 at `http://127.0.0.1:4173/` with direct state injection across all three scenario families:
  - `temperate_plains` / `Broken Frontier`: a forced `storm + banditRaid` produced `3` fronts with weather pressure `0.67`; the raid targeted `west lumber route` with pressure `1.88`, severity `high`, weather overlap `14`, and a secondary impact at `(48, 26)`
  - `fortified_basin` / `Gate Bastion`: a forced `storm + banditRaid` produced `3` fronts with weather pressure `0.78`; the raid targeted `south granary` with pressure `1.81`, severity `high`, and a secondary impact at `(61, 37)`
  - `archipelago_isles` / `Island Relay`: a forced `storm + tradeCaravan` produced `3` fronts with weather pressure `0.70`; after reclaiming the depot anchor, the caravan targeted `central relay depot` with weather overlap `13`, low pressure `0.45`, and `yield x1.17`
- Verified the rendered Event Queue HTML during the island-relay check, which now reports a concrete detail line instead of only raw type/status text:
  - `central relay depot | low pressure 0.45 | weather overlap 13 | contested 13 | defense 2 | yield x1.17`

**Tests added or changed**

- Extended `test/weather-spatial.test.js` so Stage 9 now verifies:
  - front arrays exist
  - per-tile penalty maps are populated
  - weather pressure scores are non-zero when fronts are active
- Extended `test/world-event-spatial.test.js` so Stage 9 now verifies:
  - bandit raids carry spatial pressure / severity metadata
  - migration writes a pressure value into herbivore context
  - caravan reward drops under weather-contested depot conditions
- Extended `test/world-explain.test.js` so interpretation now verifies:
  - front counts and pressure in weather summaries
  - spatial-pressure summaries in event insight
  - updated tile weather wording around per-tile path cost
- Extended `test/snapshot-service.test.js` so Stage 9 weather-front and spatial-pressure state survives serialize / restore.
- Added a new regression in `test/progression-system.test.js` so concentrated spatial pressure can now drive a reroute-oriented pacing hint.
- Full regression status after Stage 9:
  - `npm test` -> `103/103` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation on `http://127.0.0.1:4173/` confirmed Stage 9 behavior across:
  - `temperate_plains` / `Broken Frontier`
  - `fortified_basin` / `Gate Bastion`
  - `archipelago_isles` / `Island Relay`
- In those deterministic checks:
  - weather fronts now publish concrete focus-zone labels and pressure values instead of only a generic hazard count
  - bandit raids can now escalate into a second impact when the frontier lane is both exposed and weather-stressed
  - caravan value now responds to depot readiness and contested weather overlap instead of staying flat
  - the active event UI now shows target zone, pressure, overlap, defense, and reward details instead of only type / status

**Remaining risks**

- Weather and event pressure is now spatial and severity-based, but raids still use a lightweight one-to-two-tile sabotage model rather than a richer multi-wave assault.
- Trade caravans now read contested depot lanes, but they still do not form coordinated multi-agent caravans with true route-level escort or retreat behavior.
- Objective hints now understand spatial pressure, but they still intentionally prioritize hard logistics failures such as isolated worksites or overloaded depots when those issues are simultaneously present.

### Stage 10. LLM Director / NPC Policy / Prompt-Context Quality

**Goal**

- Tighten the AI contract so environment directives and group policies are constrained, explainable, and behaviorally relevant, then verify that those richer fields show up both in the runtime UI and in actual target selection instead of stopping at prompt text.

**Problems found before work**

- The Stage 9 AI layer already had working fallback and proxy plumbing, but the contract between prompts, validation, guardrails, and runtime behavior was still too loose.
- Prompt payloads were still relatively thin and duplicated between the browser client and proxy path, so fallback and live-AI requests did not always carry the same scenario, logistics, ecology, and objective context.
- Group policies exposed `targetPriorities`, `focus`, and other explanation fields, but those fields were not yet consistently enforced or used deeply enough at runtime. In several cases they were closer to labels than behavior.
- The parsed AI panel could still show weak or blank narrative fields when the fallback path returned only a minimal directive, which undermined the project's interpretable-AI claim.
- Legacy visitor-policy handling still had a correctness gap: when older generic visitor policies were split into `traders` and `saboteurs`, extra keys could survive longer than they should.

**Iteration A fixes**

- Reworked the AI contract in `src/config/aiConfig.js` so each group now has an explicit policy contract:
  - supported intent keys
  - supported target-priority keys
  - default focus / summary / steering guidance
  - canonical group-id normalization for legacy names such as `visitors`
- Extended `ResponseSchema` so policies can validate richer outputs:
  - `focus`
  - `summary`
  - `steeringNotes`
  - numeric `targetPriorities`
  - canonical group IDs for both policy batches and state targets
- Reworked `Guardrails` so unsupported intent keys and target-priority keys are pruned back to the per-group contract instead of silently surviving into runtime state.
- Added guarded fallback defaults for environment directives, so partial directives now still render non-empty `focus`, `summary`, and `steeringNotes` text instead of producing a blank parsed panel.
- Reworked `NPCBrainSystem` runtime normalization so split `traders` / `saboteurs` policies are sanitized against the destination contract even when the source data came through a legacy generic visitor shape.

**Iteration B expansions**

- Added `PromptPayload.js` as a shared browser / proxy prompt-payload builder, then wired both `LLMClient` and `server/ai-proxy.js` to use it for:
  - environment-director requests
  - NPC-policy requests
- Expanded both prompt payloads to carry the authored world context the AI layer actually needs:
  - scenario family and summary
  - objective title, description, progress, and hint
  - frontier route / depot status
  - logistics bottlenecks
  - ecology pressure
  - spatial pressure
  - gameplay recovery state
  - operational highlights and key issues
- Reworked `PromptBuilder` fallback logic so fallback directives and policies now consume the same richer world summary instead of emitting one generic steering shape.
- Reworked `WorldSummary` so the AI layer now receives a structured narrative of scenario, objective, gameplay, frontier, logistics, ecology, operations, and spatial pressure rather than only coarse resource counts and entity totals.
- Updated the authored prompt source files:
  - `src/data/prompts/environment-director.md`
  - `src/data/prompts/npc-brain.md`
  so they now explicitly request explanation fields and respect the new per-group contracts.

**Iteration C tuning and debugging**

- Reworked target selection in the actual unit systems so policy target priorities now affect real runtime scoring:
  - `WorkerAISystem` now scores warehouses, roads, depots, frontier pressure, safety, ecology pressure, and warehouse load when choosing work or delivery targets
  - `VisitorAISystem` now scores traders and saboteurs against defended lanes, warehouse value, depot readiness, route support, chokepoints, and exposed frontier targets
  - `AnimalAISystem` now uses target priorities for herbivore grazing and predator hotspot selection instead of only relying on generic local heuristics
- Added runtime debug metrics for worker target scoring so the Entity Focus panel can now expose:
  - policy target score
  - frontier contribution
  - depot contribution
  - warehouse-load contribution
  - ecology contribution
- Updated `AIDecisionPanel` and `EntityFocusPanel` so the new structured fields are visible in play, not just hidden inside debug state:
  - environment `focus`, `summary`, and `steeringNotes`
  - policy `focus`, `summary`, and `steeringNotes`
  - top target priorities
  - target-selection diagnostics on selected entities
- During browser validation I found one real UI regression: the parsed environment panel could still show blank explanation text on partial fallback directives. I fixed that by pushing guarded narrative defaults into the environment-directive normalization path instead of leaving the UI to guess.
- Manual validation also showed an important current limitation: across the three authored opening scenarios, fallback policy currently converges on the same early-game logistics-repair emphasis (`contested logistics lane` / `depot throughput`). That is acceptable for correctness, but it means Stage 10 currently delivers stronger explainability and stronger action-level relevance than true scenario-divergent AI personality.

**Tests added or changed**

- Updated `test/ai-contract.test.js` so Stage 10 now verifies:
  - richer schema validation for `focus`, `summary`, and `steeringNotes`
  - contract pruning of unsupported intent and target keys
- Updated `test/policy-fallback-state-template.test.js` so fallback policy coverage now includes the richer narrative fields and expanded target priorities.
- Added `test/prompt-payload.test.js` to verify that shared environment / policy payload builders include operational highlights, group contracts, and explanation-field expectations.
- Added `test/policy-target-priority-behavior.test.js` to verify that worker delivery targeting can now prefer a frontier warehouse when route-relief priorities demand it.
- Full regression status after Stage 10:
  - `npm test` -> `106/106` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation used the local production preview at `http://127.0.0.1:4173/` and covered the three authored scenario families:
  - `Temperate Plains` / broken-frontier start
  - `Fortified Basin` / chokepoint start
  - `Archipelago Isles` / fragmented-water start
- In all three runs, after `Start Run`:
  - `AI Decisions (Parsed)` rendered non-empty environment `focus`, `summary`, and `steeringNotes`
  - worker policy entries rendered non-empty `focus`, `summary`, and target priorities
  - `Entity Focus` rendered non-empty `Policy Focus`, `Policy Summary`, and `Target Selection` diagnostics for a selected worker
- A direct runtime inspection of the rendered panels confirmed that the new fields are coming from the actual guarded AI state, not only from static copy:
  - `aiDecisionPanel.lastHtml` contained the environment directive explanation fields
  - `entityFocusPanel.lastHtml` contained the selected worker's policy explanation and target-scoring diagnostics
- The current fallback policy converged to the same early-game logistics emphasis across all three scenario families:
  - environment focus: `contested logistics lane`
  - worker policy focus: `depot throughput`
  This is a valid result for the current authored opening states, but it also marks the next real opportunity for improvement.

**Remaining risks**

- Stage 10 tightened the AI contract and made policy metadata behaviorally relevant, but fallback still converges too quickly on one shared logistics-repair doctrine across multiple scenario families.
- The live-AI and fallback paths now share payload structure, but I only validated the fallback execution path in-browser; truly comparing live-LLM divergence still depends on stable external credentials and a reachable proxy target during the validation session.
- Policy target priorities are now wired into behavior, but only the worker side currently has strong player-visible target-scoring telemetry. Traders, saboteurs, and animals still expose less numeric scoring detail in the UI than workers do.

### Stage 11. Interpretability / HUD / Inspector / AI Trace Consolidation

**Goal**

- Collapse the current mix of debug strings and local panel summaries into one shared interpretability layer, so map state, unit state, AI state, and player-facing telemetry all tell the same causal story about what is blocking progress and what the player should do next.

**Problems found before work**

- By Stage 10 the project had a lot of explanation text, but it was still fragmented. The HUD, Inspector, AI panel, Entity Focus overlay, and Developer Dock were all summarizing the run differently.
- The parsed AI panel showed valid directive and policy metadata, but it still stopped short of connecting that metadata back to the current objective blocker and the evidence already visible elsewhere in the game.
- The HUD warning line could still surface low-level internal warnings such as dropped infeasible state targets, which are useful for developer debugging but not for player-facing explanation.
- Inspector and Entity Focus each had useful local detail, but they did not consistently anchor that detail back to one global headline, so the UI could still feel like several adjacent debug panels rather than one interpretable system.
- Browser validation also exposed a runtime bug that made the problem worse: `AnimalAISystem` could throw `distanceSq is not defined`, which leaked straight into the HUD warning field and crowded out the actual gameplay explanation.

**Iteration A fixes**

- Added a shared interpretability digest in `src/ui/interpretation/WorldExplain.js`:
  - `getAiInsight(state)`
  - `getCausalDigest(state)`
- The new digest now computes one shared player-facing explanation from:
  - current objective
  - frontier route / depot status
  - logistics bottlenecks
  - traffic hotspots
  - weather pressure
  - event pressure
  - active AI focus
- Reworked `HUDController` so the core HUD now uses that shared digest for:
  - objective headline
  - AI summary
  - action text
  - warning text
- Added filtering for low-level AI feasibility warnings so internal guardrail noise no longer overrides the main player-facing warning line.
- Fixed the live runtime bug in `AnimalAISystem` by restoring a local `distanceSq` helper, which removed the recurring `AnimalAISystem failed: distanceSq is not defined` error from browser playtests.

**Iteration B expansions**

- Reworked `AIDecisionPanel` so it now begins with a `Live Causal Chain` block that explains:
  - current severity
  - the top headline
  - the recommended next move
  - the current warning focus
  - the AI summary
  - the supporting evidence list
- Reworked `InspectorPanel` so it now includes:
  - a `System Narrative` block when no tile is selected
  - a `Why It Matters` block when an entity is selected
  - the same global headline and AI narrative that already appear in the parsed AI panel and HUD
- Reworked `EntityFocusPanel` so selected units now show:
  - global headline
  - global warning
  - local policy explanation
  - local target-selection diagnostics
  in one contiguous narrative instead of splitting global and local reasoning across separate panels.
- Reworked `DeveloperPanel` AI trace output so it now starts with a short narrative header before the raw trace list, which keeps the dock useful for diagnosis without making it the only place where the explanation is coherent.

**Iteration C tuning and debugging**

- Tuned the interpretability priority order in the shared digest so the UI now prefers real colony blockers in this order:
  - isolated worksites
  - broken routes
  - unreclaimed depots
  - severe raids
  - depot throughput
  - traffic hotspots
  - weather fronts
  - ecology pressure
- Re-ran Stage 11 browser validation and found that low-level AI feasibility warnings could still displace the global narrative in the HUD. I fixed that by suppressing those warnings in the player-facing HUD while keeping them available in developer telemetry.
- Re-ran browser validation after the fix and confirmed that:
  - the HUD warning line returned to map/logistics pressure instead of internal feasibility spam
  - the parsed AI panel, Inspector, Entity Focus, and AI trace all kept the same causal headline
  - the `AnimalAISystem` runtime error no longer appeared
- One remaining design constraint became clearer during this pass: early authored starts still intentionally spend a lot of time in route-repair / depot-throughput logic, so Stage 11 improves coherence much more than it increases scenario-to-scenario narrative diversity. That diversity still depends on later scenario and AI work, not just better text.

**Tests added or changed**

- Added `test/ai-decision-panel.test.js` to verify that the parsed AI panel now renders a `Live Causal Chain` plus the shared AI / logistics explanation fields.
- Extended `test/world-explain.test.js` so Stage 11 now verifies that the new shared interpretability digest combines frontier pressure and AI focus into one evidence-backed narrative.
- Extended `test/hud-controller.test.js` so Stage 11 now verifies:
  - HUD objective and AI lines now reflect the shared causal digest
  - low-level AI feasibility warnings are suppressed in favor of player-facing narrative text
- Full regression status after Stage 11:
  - `npm test` -> `109/109` passing
  - `npm run build` -> passing

**Manual validation**

- March 10, 2026 browser validation used the local production preview at `http://127.0.0.1:4173/` and covered:
  - `Temperate Plains` / broken-frontier start
  - `Fortified Basin` / chokepoint start
  - `Archipelago Isles` / fragmented-water start
- In all three runs, after `Start Run`:
  - the HUD objective line included a scenario-specific headline rather than only the raw objective title
  - the parsed AI panel rendered `Live Causal Chain`
  - the Inspector rendered `System Narrative` or `Why It Matters`
  - Entity Focus rendered `Global Headline` and `Global Warning`
  - the Developer Dock AI trace rendered a `Narrative:` header before raw trace lines
- The final browser verification also confirmed two cleanup points:
  - the HUD no longer surfaced `Dropped infeasible state target ...` as the main warning text
  - the `AnimalAISystem failed: distanceSq is not defined` error no longer appeared in live play

**Remaining risks**

- Stage 11 makes the UI much more coherent, but the shared narrative is only as good as the current scenario pressure model. If future systems regress into noisy or low-value pressure, the explanation layer will faithfully reflect that weakness.
- The player-facing HUD now suppresses one class of low-level AI feasibility warnings, but there may still be other purely technical warnings that should eventually be downgraded to developer-only telemetry.
- The current digest is intentionally concise. It is strong at identifying the main blocker and next move, but it does not yet expose more advanced comparative reasoning such as “why this route over that route” in one single combined view.

### Stage 12. Performance / Soak / Replay / Snapshot / Build Size

**Goal**

- Turn Stage 12 into a real release gate instead of a vague promise by adding long-run simulation coverage, restoring the broken full-verification chain, hardening snapshot roundtrips, and reducing bundle-risk noise to a controlled, explained state.

**Problems found before work**

- The project already had unit tests, UI layout checks, and a performance baseline script, but there was still no full-stack soak runner that exercised authored scenarios through progression, weather, events, AI, wildlife, logistics, mortality, and replay/snapshot state in one continuous pass.
- `verify:full` was not actually trustworthy because `package.json` still referenced `scripts/verify-a3.mjs`, but that script was missing from the repository. The chain looked complete while still failing at the last step.
- Snapshot tests covered one serialize / restore cycle, but they still left room for repeated roundtrips to silently degrade nested `Map` / `Set` structures, weather-front metadata, ecology pressure data, and AI policy state.
- The production build was functionally correct, but the chunk story was still too noisy. A single large bundle warning did not help distinguish between acceptable vendor weight and real application-chunk risk.

**Iteration A fixes**

- Added `scripts/soak-sim.mjs`, a deterministic Stage 12 soak runner that boots the full authored game state, enables the active session, steps the complete simulation stack for all three scenario families, and fails immediately on:
  - non-finite core metrics
  - system-level error warnings
  - snapshot restores that lose `Map` / `Set` runtime structures
- Expanded `scripts/verify-full.mjs` into a six-step gate:
  - unit tests
  - UI layout tests
  - production build
  - performance baseline
  - soak simulation
  - HW03 proof verification
- Restored the missing `scripts/verify-a3.mjs`, making `verify:full` honest again. The new script validates:
  - stored live-AI proof
  - stored fallback proof
  - freshly generated performance baseline CSV
  and writes a machine-readable summary to `docs/assignment3/verification-summary.json`.

**Iteration B expansions**

- Added a stronger repeated roundtrip regression in `test/snapshot-service.test.js`, so Stage 12 now explicitly verifies that repeated serialize / restore cycles preserve:
  - AI group policies
  - AI group state targets
  - weather hazard Sets and front summaries
  - ecology hotspot metadata
- Added generated Stage 12 metrics targets in `docs/assignment4/metrics/`, so the long-run scenario pass and performance baseline now leave behind concrete evidence instead of only console output.
- Reworked `vite.config.js` to split the build into a small set of explicit chunks that match the current architecture instead of over-fragmenting the simulation:
  - `vendor-three`
  - `ui`
  - main runtime chunk

**Iteration C tuning and debugging**

- I first tried a deeper chunk split across simulation and world layers, but Rollup surfaced circular-chunk problems. I rolled that back and kept only the stable, low-risk split that actually improves the build story without destabilizing runtime imports.
- Tuned the build warning gate to `650 kB` so the verified `vendor-three` chunk is treated as an acknowledged vendor cost instead of a false-positive regression, while still leaving headroom small enough to catch future bloat.
- Re-ran the full chain after restoring `verify:a3`; this closed the real Stage 12 blocker and confirmed that the repo now supports an end-to-end local verification path instead of several disconnected scripts.

**Tests added or changed**

- Added `scripts/soak-sim.mjs` and wired it into `package.json` as `npm run soak:sim`.
- Restored `scripts/verify-a3.mjs` and wired its summary output into the Stage 12 verification artifact set.
- Extended `scripts/verify-full.mjs` so Stage 12 now blocks on the soak runner and HW03 artifact verification instead of only tests/build/perf.
- Extended `test/snapshot-service.test.js` so Stage 12 now verifies repeated roundtrips preserve nested Maps, Sets, AI state, weather metadata, and ecology metadata.
- Full regression status after Stage 12:
  - `npm test` -> `110/110` passing
  - `npm run build` -> passing
  - `npm run soak:sim` -> passing
  - `npm run verify:full` -> passing

**Manual validation**

- March 10, 2026 browser smoke validation used the local Vite server at `http://127.0.0.1:4173/` with AI disabled and covered the three authored scenario families:
  - `Temperate Plains` / `Broken Frontier`
  - `Fortified Basin` / `Gate Bastion`
  - `Archipelago Isles` / `Island Relay`
- In those smoke checks, each regenerated run stayed in `phase=active`, advanced simulation time, and surfaced stable system summaries instead of runtime errors:
  - `Broken Frontier`: `tick=20`, `timeSec=0.7`, `weather=west lumber route, east ruined depot`, `traffic=lanes clear`, no error warnings
  - `Gate Bastion`: `tick=25`, `timeSec=0.8`, `weather=north timber gate, south gate`, `logistics=no warehouse anchors online`, no error warnings
  - `Island Relay`: `tick=20`, `timeSec=0.7`, `weather=harbor relay causeway, east fields causeway`, `logistics=no warehouse anchors online`, no error warnings
- Command-line validation on March 10, 2026 confirmed:
  - `npm run build` now emits three explicit JS chunks:
    - `ui` = `184.08 kB`
    - `index` = `217.24 kB`
    - `vendor-three` = `612.75 kB`
  - `npm run soak:sim` completed all three scenarios at `2703` ticks each with `0` deaths
  - `npm run verify:full` now completes all six steps successfully, including the restored HW03 proof gate
  - `npm run verify:a3` validated `24` performance-baseline rows across all `6` terrain templates and consumed the freshly generated `docs/assignment4/metrics/perf-baseline.csv` artifact

**Remaining risks**

- The soak runner is now a real full-stack stability gate, but it still runs at authored default population levels rather than at the heaviest benchmark loads. Extreme late-stage crowding remains a Stage 12 follow-up opportunity if the entity ceiling increases again.
- Bundle risk is now explained and partitioned, but `vendor-three` is still a large chunk. The current split is stable; deeper code-splitting should only happen if it can be done without reintroducing circular-chunk fragility.
- `verify:a3` intentionally validates stored evidence offline instead of refreshing live-AI proof on every run. That makes Stage 12 reproducible, but a future release pass should still refresh live-AI evidence when stable credentials are available.

### Stage 13. Deployment / Submission Evidence / Buffer Freeze

**Goal**

- Close the roadmap without adding new gameplay systems: finalize the local release gate, verify the production preview, and make the HW04 document self-consistent about what is shipped, what is verified locally, and what still remains outside this submission.

**Problems found before work**

- By the end of Stage 12 the project had strong local verification, but it still lacked one final submission-specific gate that checked the built app, the HW03 proof artifacts, and the completeness of `a4.md` itself in one place.
- The repo also needed one explicit final statement about deployment scope. A dead hosted link had already been removed earlier, but the submission still needed a final freeze note that the authoritative artifact is the locally verified build, not an unverified public host.
- Stage 12 produced the right evidence locally, but there was still no dedicated final-check command for the release pass.

**Iteration A fixes**

- Added `scripts/release-check.mjs` plus `npm run release:check`.
- The new release gate checks:
  - `dist/index.html` exists after build
  - HW03 live-AI proof exists
  - HW03 fallback proof exists
  - HW03 performance proof exists
  - `a4.md` still contains the required top-level report sections
  - `a4.md` now contains Stage 1 through Stage 13

**Iteration B expansions**

- Updated this HW04 report to include the final Stage 13 freeze section rather than stopping at Stage 12.
- Kept the deployment story explicit and honest:
  - the local production build is the authoritative submission artifact
  - no new public deployment URL is claimed in this report
  - locally generated Stage 12 metrics remain available under `docs/assignment4/metrics/` during verification runs
- Preserved the rule that the assignment prompt file remains untouched while `a4.md` acts as the only evolving HW04 implementation record.

**Iteration C tuning and debugging**

- Re-ran the full verification chain before the final release check, then validated that `release:check` succeeds only after build + evidence + documentation are all in place.
- Performed one more production-preview smoke pass on March 10, 2026 against a local built app instead of relying only on the dev server, so the final freeze reflects the actual submission artifact.
- Kept the final Stage 13 changes intentionally narrow: no new simulation features, only release gating, QA, and documentation closure.

**Tests added or changed**

- Added `scripts/release-check.mjs` and `npm run release:check` as the final submission gate.
- Stage 13 does not add new gameplay tests; it adds a release-integrity gate on top of the Stage 12 verification chain.
- Final release status after Stage 13:
  - `npm run verify:full` -> passing
  - `npm run release:check` -> passing

**Manual validation**

- March 10, 2026 final release smoke validation used a local production preview of the built app and covered:
  - `Temperate Plains` / `Broken Frontier`
  - `Fortified Basin` / `Gate Bastion`
  - `Archipelago Isles` / `Island Relay`
- In that final preview pass:
  - each scenario loaded from the built app without startup failure
  - the run could enter `phase=active`
  - simulation time advanced without system-level error warnings
  - the authored objective title for each scenario still appeared in the final artifact
- Final command-line freeze check on March 10, 2026 confirmed:
  - `npm run verify:full` passed end to end
  - `npm run release:check` passed
  - the production build exists locally
  - HW03 proof files and the final HW04 write-up are present together

**Remaining risks**

- This submission still does not claim a new hosted public deployment because I did not have a stable, same-day verified remote target to report. The local production build is the authoritative artifact for this submission.
- `release:check` validates completeness and local artifacts, but it does not upload screenshots, video, or remote deployment evidence automatically.
- The final build is stable and locally verified, but future work should still revisit bundle weight, heavier crowd-load soak tests, and a refreshed live-AI evidence capture when external credentials are available.

## GitHub

https://github.com/DaizeDong/Project-Utopia
