# C1 — Code Architect — Round 11 (Tier C quick, NO browser)

Persona: Code Architect. R10 base d2a83b5, head 652220f. 5 commits, +1904/-65 LOC across 24 files (production delta ~+325/-48 LOC across 9 src files; rest plan/test/changelog).

## Grade distribution Δ vs R10

R10 baseline: 7A / 16B / 3C / 2D, +2 delta (1C → 2B carry).
R11 standing: **7A / 18B / 3C / 2D** (net +2B from PCC + PDD; PAA/PBB/PEE preserve B-tier; D unchanged).

- Plan-PBB-recruit-flow-fix: **B**. Surgical 2-line fix at `WorkerAISystem.js:855` warehouse-unload + mirror at `:520` bootstrap-no-warehouse direct-deposit path. Scoped to "food" by explicit comment (gate is the load-bearing surface). Defensive `recruitTotal: 0` init in EntityFactory metrics shape closes the undefined→NaN window. No new helpers, no new state — restores a missing call to an existing helper.
- Plan-PAA-game-over-copy: **B**. Pure copy + 2 textContent role-swaps on existing DOM nodes (`#overlayEndTitle` ↔ `#overlayEndReason`). `END_TITLE_BY_TIER` literals reworded; tier-label string built inline (`devTier.charAt(0).toUpperCase() + devTier.slice(1)`) — micro-duplication risk if tier set grows, but contained. `data-dev-tier` attribute preserved → CSS/test contract intact.
- Plan-PCC-combat-rebalance: **B**. Five new BALANCE.* knobs (workerAttackDamage, workerNonGuardAttackCooldownSec, saboteurMaxHp, saboteurAttackDamage, saboteurAttackCooldownSec) + two re-tunes (meleeReachTiles, predatorAttackDistance). One ternary on `worker.role === "GUARD"` in WorkerStates FIGHTING tick. New `findAdjacentWorkerForSaboteur` helper in VisitorAISystem.js is a clean module-private factor (40 LOC, pure, single call site, explicit cooldown gate via existing `attackCooldownSec` field). EntityFactory swap from `wallMaxHp` to dedicated `saboteurMaxHp` decouples a previously-hidden coupling — pure win. CHANGELOG explicitly notes the v0.8.5 saboteur HP comment is now stale.
- Plan-PDD-smart-pathing: **B**. Two-bug fix is the round's biggest piece. RoadPlanner gains `roadAStar(..., { allowBridge })` flag + dual-search `planRoadConnections` picks lower of `scorePath(pathLand)` vs `scorePath(pathBridge)`. New `scorePath` helper (~15 LOC) uses inline `TRAFFIC_AMORTIZATION = 50` + `/100` magic divisor — comment explains the order-of-magnitude calibration but it's the round's most undertested constant (no sensitivity test). BridgeProposer rewrites the candidate scan from 1-tile-pinch → multi-tile shoreline-pair (~80 LOC added including memoized BFS via `landDetourCache`). Five new module-scope inline constants (`RADIUS_TILES=8`, `DETOUR_RATIO_THRESHOLD=1.5`, `SHORE_STRIDE=2`, `BRIDGE_STEP_COST=2.0`, `TRAFFIC_AMORTIZATION=50`) — none promoted to BALANCE per hard-freeze. The `landDetourCache` is a per-call `Map`, properly scoped to the function — no cross-tick leakage.
- Plan-PEE-goal-attribution: **B**. detectMilestones now takes `runtime` as second arg; depot-aware override branch fires only on `kind === "first_warehouse"` with explicit `runtime` truthiness guard. `\bruined\s+/i` regex strip is the only piece of new copy-mangling — narrow and well-scoped. Default rule strings reworded so the no-runtime/no-depot path also drops the "extra" misnomer. Single internal-call signature change at line 898; emit format `${label}: ${message}` reuses both vars consistently.

## Top-3 debt

1. **Five new module-scope magic numbers in PDD** (BridgeProposer + RoadPlanner) — `RADIUS_TILES=8`, `DETOUR_RATIO_THRESHOLD=1.5`, `SHORE_STRIDE=2`, `BRIDGE_STEP_COST=2.0`, `TRAFFIC_AMORTIZATION=50`. Hard-freeze precluded BALANCE promotion, but these are cross-cutting tuning knobs that will outlive the freeze. Also: `bestDistSq = 4.0` literal in `findAdjacentWorkerForSaboteur` (PCC) duplicates intent of `meleeReachTiles=2.0` (`2.0² = 4.0`) but doesn't reference the BALANCE knob — silent drift if `meleeReachTiles` is re-tuned again.
2. **R9 carryover (`#writeBuilderTelemetry` extract) STILL not done** — R8→R9→R10→R11 four-round carryover. WarehouseNeedProposer twin O(workers) walks (`computeNoAccessRatio` + `computeCriticalHungerRatio`) ALSO still not folded. Both flagged in R9 and R10 feedback. R10's 5 plans hit different files entirely (combat, pathing, UX copy, telemetry helper-add, milestone) so the carryover has had four rounds of opportunity. Recommend the next utility-pass round explicitly scope these as P1 cleanup tasks.
3. **PEE's `runtime` parameter threading through `detectMilestones` is a one-deep API widening that begs for follow-on misuse** — only the `first_warehouse` branch uses it, but every future milestone rule will be tempted to peek at `runtime` (objective progress, depot count, scenario tier). Either commit to that pattern explicitly (rule.computeOverride(state, runtime) hook on the rule object) or keep `runtime` access narrowly fenced. Currently it sits midway — neither documented as a general capability nor structurally restricted.

## New debt from R10 commits

- **Two-source-of-truth for melee reach** (PCC) — `BALANCE.meleeReachTiles=2.0` for workers, `BALANCE.predatorAttackDistance=2.4` for predators, hardcoded `bestDistSq=4.0` for saboteur strikes. Three tunables for one physical concept ("how close do you need to be to hit"). Documented in PCC plan as intentional asymmetry, but worth a single-line comment block linking the three.
- **`scorePath` magic divisor `/100`** (PDD) — explained in comment as "bring traversal term into parity-of-magnitude with build-resource units" but the parity-check derivation is informal. A single sensitivity test (vary TRAFFIC_AMORTIZATION ± 50%) would catch a future regression where build-cost dominates and bridges never win even on archipelago.
- **PEE `runtime.depots` shape coupling** — `detectMilestones` now reads `runtime.depots[].label` and `runtime.depots[].ready`. If a future scenario format renames `label` → `name` or `ready` → `complete`, the regex strip and find both silently no-op (default rule strings still emit). Defence-in-depth `?? "depot"` covers the missing-label case but not the wrong-key case.
- **CHANGELOG note on v0.8.5 stale saboteur HP comment** (PCC) is correctly self-flagged — the EntityFactory.js comment was updated but downstream docs/tests asserting the wallMaxHp coupling (if any survive) would now lie. R11 grep for "wallMaxHp" + "saboteur" same-file would catch any stragglers.
- No new globals, no new circular imports, no new direct DOM access in simulation, no new hidden underscored fields beyond R9/R10's existing five (`_eventDrainBudgetTick`, `_zombieSinceSec`, `warehouseDiagnosticSinceSec`, `_starvationPhaseSeeded`). PCC's `visitor.attackCooldownSec` is the existing visitor field, not new.

## D-tier status

**2D unchanged.** ColonyPlanner monolith + ColonyPerceiver tile-walk untouched in R10. PDD's BridgeProposer rewrite (~80 LOC added to a ~150-LOC file) raised that file's complexity meaningfully but kept it tier-C — the BFS memoization is correct and the inner pair-loop is bounded by SHORE_STRIDE. Strongest piece of R10: PCC's `saboteurMaxHp` decoupling from `wallMaxHp` — fixes a hidden-coupling bug that didn't surface in tests but would have on the next wall-HP balance pass. Weakest: PDD's five-magic-number footprint, mitigated by hard-freeze and clear comments. Recommend R11 implementer pick up the (now four-round-old) `#writeBuilderTelemetry` + WarehouseNeedProposer twin-walk carryover before they ossify.
