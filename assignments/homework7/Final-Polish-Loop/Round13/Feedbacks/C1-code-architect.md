# C1 — Code Architect — Round 13 (Tier C quick, NO browser)

Persona: Code Architect. R12 base fa6cda1, head 527f460. 7 commits, +973/-57 LOC across 19 files (production delta ~+196/-22 LOC across 7 src files; rest plan/test/changelog).

## Grade distribution Δ vs R12

R12 baseline: **8A / 18B / 3C / 2D**. R13 standing: **9A / 18B / 3C / 2D** (net **+1A** from Plan-R12-glued-tokens — pure 8-LOC helper + 1-line render-string change with new `world-explain-ai-summary.test.js` covering Title-Case + colon-space contract). Other six R13 commits all settle B.

- **Plan-R12-glued-tokens (A7 #2): A.** New `titleCaseGroup` helper at `WorldExplain.js:11`, render template swapped from `${groupId}:${focus}` to `${titleCaseGroup(groupId)}: ${focus}`. Pure presentation, no state, no consumer-side breakage. 74-LOC contract test fences the regression. Textbook surgical fix.
- **Plan-R12-debug-leak-gate (A6 #1): B.** `HUDController.js:1245` branches `aiModeVal.textContent` on `isDevMode(state)` — production string collapses to `"AI online" / "AI offline"`. Reuses existing `devModeGate.js` helper (already imported), zero new state, zero new fields. Comment correctly inventories the three companion gates (WHISPER suffix, AI Log footer, [timeout] suffix) so the dev-mode contract is discoverable. Lands at B not A only because the parallel cleanups at `AIAutomationPanel.js:134` and `GameApp.js:2106` were not bundled into this commit (cross-file leak still surface-level present per the inline comment).
- **Plan-R12-stable-tier-fix (A7 #1 P0): B.** Status-tier predicate at `HUDController.js:2333-2371` widened from single-input (`threat`) to multi-input (`foodHeadroomSec`, `foodRatePerMin`, `farmsCount`, `warehousesCount`, `threat`). Reuses already-cached `_lastComputedRates` so no new instance fields. Five branches with explicit thresholds — readable. The legacy `.replace("THRIVING", "THRIVING")` chain at line 2373-2377 is now obviously dead code (uppercase-then-replace-same-string identity ops carried over from the simple version) — flag for cleanup but not regression.
- **Plan-R12-build-tab-1click (A3 P1 #1): B.** New `isPanelAreaVisible()` helper inside the IIFE at `index.html:3987`, branch logic at sidebar-tab click handler updated to `(alreadyActive && isPanelAreaVisible())` and adds `btn.focus()` on the force-show branch. Three computed-style reads per click (opacity / pointerEvents / visibility) — cheap, click-frequency only. Comment correctly documents the 1025–1440px icon-rail :focus-within dependency from R11 PGG-responsive. Clean defensive widening on the responsive layout introduced two rounds ago.
- **Plan-R12-autopilot-hitregion (A3 P1 #3 + A5): B.** Two surfaces: (1) CSS rules on `#speedControls .speed-toggle` add cursor:pointer + 32px min-height + :hover/:focus-within/:has(input:checked) affordances (~24 new CSS lines, tasteful affordance pass) and (2) `eagerSyncBanner(checked)` helper in `HUDController.js:1010` writes a placeholder `"Autopilot ON · syncing…"` text on the change event before the canonical render() pass overwrites. Uses `:has()` selector — modern-browser-only but already in use elsewhere. Lands at B not A because the placeholder string lives inline in the JS rather than promoted to the same string-table that `getAutopilotStatus()` outputs.
- **Plan-R12-wood-food-balance (A5 #1): B.** `ZeroLumberProposer.evaluate` priority dropped 95 → 75, gated by `BALANCE.maxWoodPerFarmRatio = 5` ratio check. New `BALANCE.maxWoodPerFarmRatio` constant properly hoisted to `balance.js:811` with extensive justification comment (12 lines). Touches 3 test files (`build-proposer-interface`, `build-proposer-orchestration`, `colony-director-zero-lumber-safety`) — contract test surface intact. Clean two-axis fix: priority lowering + ratio floor. Lands at B because the food>0 guard is implicit (single-line condition) rather than a named precondition; readable but not self-documenting beyond the inline comment.
- **Plan-R12-non-temperate-fallback (A5 #2): B.** New `getTemplateStartingFood(templateId)` exported from `ScenarioFactory.js:163` mirroring the existing `getTemplateStartingResources` shape. New module-frozen `STARTING_FOOD_BY_TEMPLATE` table with 6 entries. `EntityFactory.js:813` consumes via nullish-coalesce fallback to `ALPHA_START_RESOURCES.food`. Clean parallel structure to the wood-table — no new pattern invented. Wood-table values also bumped (6 entries updated). Eight numeric BALANCE-style values inline in the table (320/360/380 food, 34/48 wood) — these are template-specific tuning so promotion to BALANCE wouldn't make sense (they're already in a frozen factory module). Lands at B because the food/wood tables are now twin parallel structures; one round from now a `STARTING_RESOURCES_BY_TEMPLATE` consolidation may be warranted (single 6×N table vs. two parallel 6×1 tables).

## Top-3 debt

1. **R9 carryover (`#writeBuilderTelemetry` extract + WarehouseNeedProposer twin O(workers) walks) STILL not done** — R8→R9→R10→R11→R12→**R13** six-round carryover. R12 was hard-freeze-closeout, R13 is a Tier C polish round; neither touched the carried files. Recommend formal punt to v0.10.2 with an explicit deferral commit referencing the carryover history.
2. **HUDController.js layering creep** — R12 adds two more responsibilities to the file already carrying StatePlanner telemetry, autopilot binding, debug-mode gating, and rate trend cache. The status-tier predicate at line 2333 now reads four distinct domains (threat / food rate / building counts / time) inline; the eager-sync banner adds a third onChange handler path. File is now ~2400 LOC. Approaching the same multi-responsibility threshold SceneRenderer hit in R11. Worth a v0.10.2 split (status-tier predicate → `colonyHealthTier.js`, autopilot handlers → `HUDAutopilotBindings.js`).
3. **STARTING_WOOD_BY_TEMPLATE + STARTING_FOOD_BY_TEMPLATE parallel structure** — R12 introduces the food table mirroring the wood table from R4. Two `Object.freeze`d 6-key tables plus two near-identical accessor functions (`getTemplateStartingResources` and `getTemplateStartingFood`). Next time a third per-template starting resource needs an override (stone? herbs?) this becomes triplet duplication. Consolidate now to `STARTING_RESOURCES_BY_TEMPLATE: { temperate_plains: { wood: 35, food: 320 }, ... }` with a single `getTemplateStartingResources(templateId)` returning the full record.

## New debt from R12 commits

- **Dead `.replace()` chain in stable-tier badgeText at HUDController.js:2373-2377** — `.replace("THRIVING", "THRIVING")` etc. are literal identity operations carried over from when the status string was constructed differently. Three lines of harmless dead code. Trivial cleanup.
- **Eager-sync placeholder string ("Autopilot ON · syncing…") lives inline in HUDController.js:1014** — the canonical autopilot status string is built by `getAutopilotStatus()` (imported from `getAutopilotStatus.js`). The placeholder bypasses that contract. If `getAutopilotStatus()`'s output format ever changes (e.g. adds an icon prefix), the eager placeholder will look inconsistent for one render frame. Move the placeholder string to the same module that owns the canonical strings.
- **`isPanelAreaVisible()` reads three computed styles per sidebar tab click** in `index.html:3987` — fine at click-frequency, but `getComputedStyle` is a forced-layout call. If the responsive icon-rail breakpoint ever expands to cover broader viewport ranges, consider caching the visibility on resize/transitionend.
- **`BALANCE.maxWoodPerFarmRatio` is named for "per farm" but compares against food stockpile, not farm count** — the constant name implies `wood / farms` but the actual check is `wood > food * ratioCap`. Rename to `maxWoodToFoodRatio` to match what the predicate computes. Comment is accurate; only the identifier is misleading.
- **Status tier predicate is unguarded against `_lastComputedRates === null`** at HUDController.js:2341 — destructure path uses `?? {}` so it survives, but `Number.isFinite(computedRates.food)` will be false on cold-boot (no rates cached yet) and `foodRatePerMin = 0` falls into the `>= -0.05` branch → `+Infinity` headroom → STABLE/THRIVING evaluated normally. Likely correct for the cold-boot frame but worth a comment confirming intent.
- **Two new comment-marker patterns in HUDController.js** — `// R12 Plan-R12-stable-tier-fix (A7 #1 P0):` and `// R12 Plan-R12-debug-leak-gate (A6 #1 + A7 #3 P0):`. Consistent with R11/R12 conventions. Now ~30 round-tagged comment markers in HUDController.js. Not yet noise but trending; consider a CHANGELOG-only convention for future rounds.
- No new globals, no new circular imports. ZeroLumberProposer correctly imports BALANCE via the relative path matching its sibling proposers.

## D-tier status

**2D unchanged.** ColonyPlanner monolith + ColonyPerceiver tile-walk untouched in R12. HUDController.js continues to absorb new responsibilities (now status-tier predicate + eager autopilot sync on top of R10/R11's debug gating + LLM degrade detector); creeping toward D-tier classification by file size and multi-responsibility. Strongest piece of R12: **Plan-R12-glued-tokens** — 8-LOC helper + 1-line render swap + 74-LOC contract test, textbook surgical-fix-with-fence pattern earning the +1A delta. Weakest: **Plan-R12-non-temperate-fallback** parallel-table duplication (food vs wood) is the seed of future debt unless consolidated in v0.10.2. Recommend the v0.10.2 cleanup brief explicitly scope (a) the four-round-old `#writeBuilderTelemetry` carryover, (b) HUDController.js multi-responsibility split, and (c) `STARTING_RESOURCES_BY_TEMPLATE` table consolidation — all three trace to consistent patterns of additive defensive widening that have now reached the threshold where extraction beats further accumulation.
