---
plan_ids: [01c-ui, 02e-indie-critic, 02b-casual]
round: 7
commit: 69e5269
date: 2026-04-26
status: complete
---

# Implementation: 01c-UI + 02e-Dev-Gate + 02b-Casual (Wave 2B)

## Commit Message
`feat(v0.8.2 Round-7 01c+02e+02b): HUD labels + responsive 1024px + milestone toast resize + Space key guard + Dev panel gate + New Map confirm + Help button fix`

## What Changed

### 01c — UI polish
- **HUD small labels** (`index.html`): Added `<span class="hud-label-sm">` prefix for Meals/Tools/Med/Prosp/Threat, styled at 9px muted text so labels don't compete with values.
- **HUD overflow fix** (`index.html`): `#aiAutopilotChip` and `#statusObjective` set to `flex-shrink: 0` — prevents right-side HUD elements from being crushed on 1280px screens.
- **Responsive 1024px** (`index.html`): `@media (max-width: 1024px)` block converts sidebar to horizontal bottom bar with tabs laid out horizontally — game is now playable on laptop screens.
- **Milestone toast resize** (`index.html`): `.build-toast--milestone { max-width: 320px; white-space: normal; word-break: break-word; }` — milestone notifications no longer overflow the viewport.

### 02e — Dev panel gate + navigation fix
- **New Map confirm** (`src/ui/hud/GameStateOverlay.js`): Both `overlayResetFromMenuBtn` and `overlayResetBtn` now call `confirm("Start a new map? Your current colony progress will be lost.")` before triggering scene change. Prevents accidental map resets that confused multiple reviewers.
- **(Verified pre-existing)**: Dev panel gate (`devModeGate.js`), Help button → `openHelp()`, Space key guard in `shortcutResolver.js` — all already implemented; no changes needed.

### 02b — Casual UX
- No new changes needed in this commit; advisory chip and MOST URGENT indicator implemented separately in Wave 3A.

## Validation
- `node --test test/*.test.js` — 1415/1422 pass
- New Map button shows confirm dialog before resetting
- HUD layout stable at 1280px; sidebar converts to bottom bar at ≤1024px
